# Freeze audit: terminal common-cap theorem and private port router

**Date:** 2026-08-03  
**Method:** pure mathematics and read-only theorem-scope audit; no finite
search or computational construction was run.

## Frozen payloads

1. [MATH_THEOREM_TERMINAL_COMMON_CAP_TWO_CROSS_RAY_RADO_GAMMOID_V2_20260803.md](MATH_THEOREM_TERMINAL_COMMON_CAP_TWO_CROSS_RAY_RADO_GAMMOID_V2_20260803.md)

   SHA256
   \(108c4b0ad2adc99d9e9c91df9f1bfdb9f816494670f7473c51e7751f134177cc\).

2. [MATH_THEOREM_REGULAR_INCIDENCE_FACTOR_PRIVATE_PORT_ROUTER_20260803.md](MATH_THEOREM_REGULAR_INCIDENCE_FACTOR_PRIVATE_PORT_ROUTER_20260803.md)

   SHA256
   \(2d64e2cb36b65706133ae1e68c6bdda80d433ccd428259860382a301538096d0\).

The unversioned first common-cap draft
[MATH_THEOREM_TERMINAL_COMMON_CAP_TWO_CROSS_RAY_RADO_GAMMOID_20260803.md](MATH_THEOREM_TERMINAL_COMMON_CAP_TWO_CROSS_RAY_RADO_GAMMOID_20260803.md)
is formatting-failed lineage and is explicitly marked **DO NOT CITE**.

## Audited conclusions

1. At one fixed common cap, with global product closure and all residual
   cross-coordinate capacities exhausted, the two occurrence-coordinate
   Rado matroids have exact common deficiency

   \[
   \delta=
   \max_{J_0\cap J_1=\varnothing}
   \left(
   |J_0|-r_{N_0}(A_0(J_0))
   +|J_1|-r_{N_1}(A_1(J_1))
   \right).
   \]

2. Frozen named background routes and adaptive background contraction use
   different residual ranks and cut offsets. The frozen/adaptive formulas
   are separated in the V2 theorem.
3. Structural-zero labels separate exactly. Empty raw menus are sufficient
   but not necessary for zero status; own-new-host routes and all admitted
   phase-specific occurrences are included before classifying a zero.
4. A left \(h\)-regular/right-at-most-\(h\) literal incidence factor plus
   one simultaneous private active-port router yields one unit of gain flow
   per claim by uniform \(1/h\) flow and integral max flow.
5. At \(h=2\), the \(ML_m\) small protected-factor theorem supplies only
   the abstract degree factor, after an explicit ticket-to-left-shore
   injection. It supplies neither the capacity-faithful physical port map,
   the literal claim-to-port prefixes, nor the typed private suffix router.
6. The existing parent/reference matching does not currently prove the
   active typed suffix rank

   \[
   r_{\Gamma_{\mathrm{suf}}^{\mathrm{type}}}(P)=|P|
   \]

   or an alternative capacity-faithful router certificate. A factor and a
   matching alone cannot imply it: a common unit supplier bottleneck gives
   unbounded deficiency while both abstract objects remain exact.

## Scope guard

The positive router statement is conditional on one materialized
cap/guard/occurrence state, a fixed compensation linkage, active physical
ports, private literal prefixes, typed or identity-safe suffixes, and
compatible two-coordinate occurrence choices. It does not prove that the
current all-\(k\) parent/reference matching realizes those hypotheses.

Transported phase 1 remains theorem input only. Nothing in these payloads
authenticates it as a native or private phase, and no unconditional
\(B(k)+O(1)\) construction is claimed.
