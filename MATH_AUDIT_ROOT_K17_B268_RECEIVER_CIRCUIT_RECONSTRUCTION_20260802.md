# Root audit: authoritative K17 `b268` receiver circuits through the active `def84` rebase

**Date:** 2026-08-02  
**Status:** complete structural reconstruction and exact rowwise socket no-go
for the two declared receiver-circuit families.  No composable packet and no
global support floor is claimed.

## 1. Literal parents and phase scope

The baseline parent is the authenticated private-bank materialization

```text
b268d1248e53d164d87bc83cf69fbd5ebd412451ac9e4a4313408167f1e637dc
  scratch/k17_phase0_retained_witness_private_basis_20260802/
    private_h_outer_materialized.tsv
```

The first supplier-descent rebase uses the independently audited `def95`
table

```text
ae44aebdae38c0ca5cb5d76ab2a6a305a67b52f17b4e395a3aedbd3d97300374
  scratch/q1_k17_b268_llr_radius_def95_20260802/iter1.table.tsv
d6dca87229b542481f4f686f851c15700939db9b952cd26ce9df3b47d19bb8f0
  scratch/q1_k17_b268_llr_radius_def95_20260802/iter1.selected.tsv
```

The active parent is the deterministic release/compensation descendant of
strict `def85`, not the earlier generic socket-regression branch:

```text
3171bf380c3a139313f1304bb1d821810cb7402c20aa99f01ab8ccdc189854f1
  scratch/q1_k17_b268_llr_release_theorem_20260802/
    def84_pareto_remove32175_add36629/table.tsv
c490cc49aef575caae9f72d478ff08320f24a3625593f2465f9b94fb7d8ce979
  .../def84_pareto_remove32175_add36629/selected.tsv
7f2dc7ae146ae6a706a2399326b293809f7ae62d34a89ac393a2301a357dab05
  .../def84_pareto_remove32175_add36629/projection.tsv
```

Phase zero always uses the literal target parent and its native owner column.
The second price is only the root-aligned `round047.s7.phase1` owner overlay.
It is a transported-opening marginal, not an authenticated carrier phase.

The socket oracle is the complete origin-bound `2,129,500`-mode potential-p
superset with the exact relaxed-nine five-cell DP.  Therefore a zero is a
sound rejection of the narrower literal native state.  A positive is only a
rowwise marginal and would still need the protected-occurrence, residual
outer, selected-parent supplier, endpoint, demand-disjointness and cyclic-cell
gates.

## 2. Exact mixed-F support-four catalogue on `b268`

A current F row means a current length-two row outside the fixed 3,899-row
original-P2 label bank.  A mixed-F C8 is a directed simple four-cycle on the
7,395 current short rows, meeting the twelve named residual anchors and using
at least one current F row.  Only directed rotation is quotiented; reversal is
not.

Two separately compiled enumerators return the same canonical set:

```text
current original-P2 rows                         3,899
current F rows                                   3,496
canonical mixed-F C8s                           15,159
by F-count                              1,914 / 5,280 / 7,965
cycles avoiding all 7,213 ticket addresses      14,986
```

The protected-clean cycles have 654 distinct mandatory P2-to-F rebuilt role
keys.  Their exact rowwise screen gives

```text
mandatory keys                         654
native phase-0 positive                232
p0 and transported-p1 positive         175
cycles retaining every mandatory key     6
cycles retaining all four rebuilt roles  0
```

The last six cycles are the six rotations/classes using anchor `23340` and
the same P2/F boundary pattern.  Every one contains another rebuilt role with
zero potential-p phase-0 sockets.  Hence no protected-address-clean primitive
mixed-F C8 is rowwise positive in both declared phases.

This no-go uses the transported phase-1 marginal to reject part of the
catalogue.  It is not a native-phase0-only theorem for all 14,986 cycles.

## 3. Exact all-P2 C10 catalogue on `b268`

The original-P2 bank has 3,899 current short rows.  Complete enumeration of
directed simple five-cycles gives exactly 580 C10s, with anchor counts

```text
23021:57  23042:127 23046:0  23070:50
23336:2   23340:173 23471:24 23850:76
23955:71  23987:0   24051:0  24055:0.
```

The catalogue uses 584 distinct rebuilt `(row,new-bottom)` keys.  Their
potential-p prices are

```text
native phase-0 positive keys                 418 / 584
p0 and transported-p1 positive keys          344 / 584
C10s with all five phase-0-positive roles       0 / 580
C10s with all five two-phase-positive roles     0 / 580.
```

Thus every C10 already contains a phase-0-zero rebuilt role in the complete
potential superset.  Later ticket, outer, supplier, endpoint and cyclic-cell
choices cannot repair any catalogue member.

Independently, anchors `23046,23987,24051,24055` have outdegree zero in the
literal all-P2 receiver graph.  They admit no all-P2 receiver circuit at any
support, not merely no C10.

## 4. Rebase to the `def95` parent

The 438 protected LLR transfers preserve every owner and root and keep 7,395
short rows, but exchange 438 short and 438 long physical rows.  Consequently
the current P2/F split becomes `3,496/3,899`.  All twelve anchors and all
7,213 protected ticket rows remain byte-identical.

The all-P2 C10 catalogue is an induced subcatalogue of the baseline one:

```text
current all-P2 C10s                              506
lost from b268                                    74
new C10s                                           0
anchor counts
  50,125,0,39,2,153,23,72,42,0,0,0
```

All 525 def95 C10 rebuilt keys are inherited from the b268 584-key ledger,
so the exact phase-0-zero no-go transports literally.

Mixed-F C8 needs local reinsertion because 438 former long rows become F:

```text
b268 C8s                                      15,159
common surviving C8s                         14,670
lost C8s                                         489
new C8s touching added short rows                26
def95 C8s                                    14,696
protected-clean def95 C8s                    14,532
```

Producer and independent def95 ID sets are byte-equal.  The def95 staged
socket screen again has `646` mandatory keys, exactly six boundary-complete
cycles, and zero cycles with all four rebuilt roles positive in both phases.

## 5. Active rebase to the authoritative `def84` parent

The exact release/compensation move from strict `def85` removes selected
edge `32175` and adds edge `36629`.  It changes only table rows
`12586,24210,13716,13720`, retains 438 row-disjoint transfers, and leaves all
7,213 protected rows literal.  Its parent-level audit is

```text
supplier rank       16814 / 16898
deficiency          84
zero heads          71
DM shore            98 / 14
p0/p1/both/either   3102 / 2260 / 1844 / 3518
```

All owner and root columns remain unchanged.  Relative to `def95`, exactly 18
short rows leave and 18 enter, none protected, and the current bank is

```text
current short rows                              7,395
current original-P2 rows                        3,498
current F rows                                  3,897
```

The mixed-F C8 rebase is an exact deletion-only update:

```text
def95 mixed-F C8s                              14,696
lost at def84                                      25
new at def84                                        0
def84 mixed-F C8s                              14,671
by F-count                              1,711 / 5,018 / 7,942
protected-clean def84 C8s                      14,507
anchor counts
  2939,1178,1308,2048,796,428,1046,1749,249,410,1574,957
```

Only removed rows `4045`, `21492`, and `22332` occur in the deleted cycles,
with incidences `1,23,1`.  A separately compiled arbitrary-parent enumerator
agrees with the production catalogue, and every surviving full payload line
is byte-identical to its `def95` line.  The protected-clean mandatory boundary
set is the identical 646-key `def95` set: 229 keys are native-phase0 positive
and 175 are also positive under the explicitly transported phase1 marginal.
The same six boundary-complete cycles survive, but zero has all four rebuilt
roles positive in both declared phases.

Every current def84 P2 row is a byte-identical literal-b268 row.  Consequently
the all-P2 C10 ledger transports as an induced filter and is exactly unchanged
from `def95`:

```text
def84 all-P2 C10s                                506
distinct rebuilt keys                            525
anchor counts
  50,125,0,39,2,153,23,72,42,0,0,0
C10s with all five phase-0-positive roles           0
C10s with all five two-phase-positive roles         0
```

No fresh potential-p pricing was required: the literal owner/root payloads,
rebuilt keys, and transported overlay are identical to the already audited
cache entries.  Parent socket totals themselves are not arbitrary rebuilt-role
coverage and were not used as a substitute for that cache.

## 6. Historical coupled supplier exchange at deficiency 94

Before the monotone chain reached the active `def84` parent, a bounded exchange
from the `def95` selection was replayed exactly on the full 11,893-edge
protected common-positive face.  It
removes global edge `311` (rows `293,627`) and adds edge `89202` (rows
`23849,19468`), retaining 438 transfers.  The resulting witness is

```text
selected SHA-256  7a1399bbbc876d007226835b457dc83a585249d751be2829fa342cc0aedc4e2e
table SHA-256     2372971ac55737a5df73d3f6f2e2548a6da8d1e7500eda26412f41d32e8658f9
supplier rank     16804 / 16898
deficiency        94
zero heads        79
DM shore          108 / 14
p0/p1/both/either 3099 / 2254 / 1840 / 3513
```

All 7,213 ticket rows are literal.  The solve required two exact activated-
Hall cuts.  This is a supplier-parent improvement, not a composable receiver
packet: the complete residual outer matching, a common occurrence-labelled
state, endpoint/demand disjointness and literal cyclic replay remain open,
and phase 1 retains the transported-only scope.

This `def94` witness is retained as historical provenance and is superseded as
the active analysis parent by the strictly stronger authoritative `def84`
release/compensation branch above.

## 7. Exact boundary of the support claim

The requested primitive mixed-F C8 and all-current-P2 C10 families contain
no composable candidate because all members fail before the downstream gates.
This does **not** prove a global next support floor.  On literal b268, mixed-F
C4s exist through ten of the twelve named anchors and most remain unpriced;
the complete C6 catalogue and support-four permutation type `2+2` are also not
closed.  Those missing faces would have to be catalogued and priced again on
the active literal-def84 row state before claiming a sharp unrestricted floor.

## 8. Frozen artifacts

The receiver campaign used the unique H100 root

```text
/home/amodo/or15/work/root_k17_b268_receiver_circuit_lane_20260802
```

with one core for structural enumeration and at most eight pinned cores for
potential-p pricing, each worker capped at 4 GiB and two CPU hours.  The final
independent audit is

```text
9e61f05525ae352ced3ed4f488546db4ff232c472eebba235a1961482ab443f3
  scratch/root_k17_b268_receiver_circuit_lane_20260802/
    frozen_remote_complete/out/final.receiver.audit.json
```

The complete output archive and its verified output manifest are

```text
8bb42cc14551ac1df0b5600a76abb159e5d22666518364e429eb097635b663f4
  scratch/root_k17_b268_receiver_circuit_lane_20260802/
    receiver_frozen_outputs_20260802.tar.gz

scratch/root_k17_b268_receiver_circuit_lane_20260802/
  frozen_remote_complete/LOCAL_OUTPUTS_MANIFEST.sha256
```

The principal def84 catalogue payloads independently hash as

```text
de29a40863f6d7b0583098dfc3e20a0a8dfeeabad91e05a7eeffdf7640988963
  frozen_remote_complete/out/def84_mixedf_receiver_c8.producer.tsv
ed8a2d4f2604755b7be5239d8d7ed93b0249d49fc2eb0f730a986abe30a50e15
  frozen_remote_complete/out/def84_all_p2_receiver_c10.independent.tsv
```

The coherent def94 positive witness is retained separately in

```text
scratch/k17_b268_llr_def94_exchange_20260802/
```

The root monotone controller and every root-managed radius directory are
outside this receiver lane and were not launched into or written.  In
particular, the earlier shared target92 directory remains outside the evidence
set because its concurrent-writer state was explicitly UNKNOWN.
