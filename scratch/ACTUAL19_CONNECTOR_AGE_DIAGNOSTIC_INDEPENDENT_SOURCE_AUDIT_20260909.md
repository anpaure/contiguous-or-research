# Independent full-source audit of the actual19 connector-age diagnostic

Date: 2026-09-09. Status: PASS. This review was completed after the single finite-frontier execution; it is a retrospective source review, not a pre-execution approval. No mathematical execution was performed by this reviewer.

Reviewed in full:

- [Checker](audit_actual19_embedded_corrected21_connector_ages_20260909.py), source SHA-256 `27388f115066d5f6270a8d8957bc3503d81ddf231aa5e65d989afeef0663a6dd` as pinned by root and the execution certificate.
- [Review plan](ACTUAL19_EMBEDDED_CORRECTED21_CONNECTOR_AGE_DIAGNOSTIC_REVIEW_PLAN_20260909.md).
- [Completed fixed-input certificate](ACTUAL19_EMBEDDED_CORRECTED21_CONNECTOR_AGES_1371_GOOD_59_BAD_CERTIFICATE_20260909.md).

## Scope and input

The raw SHA pin, decimal nonempty-mask check, exact length, complete rank-nine/rank-ten decks, and one-period-plus-three equality establish the specified carrier before any port claim is made. Triple/four-window phase, canonical Phi, strict Johnson edges, and exclusion of both one-state and two-state closed positive runs are all checked over the complete cycle. The source does not assert a new full-cube verification.

Every embedded parent lower and edge is checked under the literal map (P<<1)|(1<<20). The fixed 00D1 port family is independently generated by a balanced-parenthesis recursion and extracted by scanning the complete lower deck; their equality and Catalan formula establish completeness. The supplied count and a zero-failure outcome are not premises.

## Ages and inverse incidences

The zero-anchored cyclic pass computes exact finite ages for the active parent last coordinate and first D-coordinate. A separate backward scan from every reported port reaches its preceding absent state and verifies both ages. The actual predecessor test is equivalent to the last-bit insertion test and is checked in both directions against the literal 10D0 mask.

The inverse routine includes the empty prefix, uses the last global minimum, and checks its result by applying forward Phi. Every containing upper at every fixed port is visited; the self predecessor is excluded. The list of strict predecessors omitting the last parent bit must consist exactly of 10D0. This is an exhaustive fixed incidence identity check, not a changed incoming matching.

## Connector replay and banks

All four masks are constructed explicitly and checked against the stated bit patterns. Each of the three edges checks actual child Phi, the exact deleted present bit, strictness, and the resulting lower state. The final three parent states provide sufficient independent finite histories: deletion ages are 3, min(alpha,3)+1, and min(eta,3)+2. These determine the threshold-three decision exactly, while the separately established infinite embedded b-history and exact parent ages justify the reported full deletion ages.

Every fixed port is retained in the complete good/bad partition. Injectivity of each of the four lower banks, disjointness of all lower banks, disjointness of all three consumed-upper banks, and the new lowers' exclusion from the embedded b-present inventory are checked. The source's complete Phi identities and explicit masks also identify the first consumed upper as reused and the other two as outside that inventory.

The source correctly allows a nonzero bad count under its diagnostic PASS status. It does not claim a child factor, simultaneous global age gluing, target coverage, or a new word.

## Resources and result attribution

The hostname guard, 30 CPU-second / 45 wall-second limits, 1 GiB address-space cap, 128 MiB file cap, fresh output directory, source snapshot, raw-input snapshot, and initially noncertified status are explicit. The final certificate is written only after all checks finish. There is one source invocation, with no alternate ports, rotations, solver, or retry mechanism. The additional external wall guard and actual execution are documented by the finite-frontier certificate.

The recorded result is 1,430 fixed ports, 1,371 legal connectors and 59 failures, with no age-two port. This review accepts the checker logic and reads the persisted result; it is not an independent rerun. The pure universal proof of the age-two exclusion is separately appended in [the 0P1 embedding note](ACTUAL_PARENT_0P1_EMBEDDING_CONNECTOR_AGE_SUPPLY_AND_BAD_EDGE_CRITERION_20260909.md), Section 3.1, so that structural assertion does not depend on the observed histogram.
