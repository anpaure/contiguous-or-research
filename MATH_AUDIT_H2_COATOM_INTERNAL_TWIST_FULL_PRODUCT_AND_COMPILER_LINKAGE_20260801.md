# Independent audit of the coatom internal-twist full-product theorem and compiler gate

Date: 2026-08-01  
Lane: H2, physical reachability / nonlinear compiler  
Status: **GO** for the formal saturated integer catalogue span; **OPEN** for
nonnegative physical realization; exact compiler-linkage criterion and
smallest common-cap counterexample proved.

## 0. Verdict

The new endpoint-planted single-block order breaker is valid.  More strongly,
the pure-depth isolator in
`MATH_THEOREM_THREAD_D_COATOM_INTERNAL_BLOCK_TWIST_FULL_PRODUCT_SPAN_20260801.md`
is correct.

Let `partial_j` be the coordinate-degree map on rank-`j` target occurrences.
Under

\[
 r\ge d+4,\qquad k\ge r+5,
\]

and with every rank `r-q`, `2<=q<=d`, between `2` and `k-2`, the complete
relabelled signed catalogue generated by common-order packets and internal
one-block twists satisfies

\[
 \boxed{
 L_{\rm twist}=\bigoplus_{q=2}^{d}\ker_{\mathbb Z}\partial_{r-q}.}
 \tag{0.1}
\]

This is an **integral saturated** equality, not merely a rational rank
statement.  Thus no coupled additive invariant or congruence remains beyond
the separate point degrees at each depth.

Equation (0.1) is not a physical reachability theorem.  The canonical
two-packet isolator has literal owner collisions, and the exact common-cap
compiler remains governed by augmenting linkage rather than by occurrence
span.

## 1. Independent pure-depth calculation

Use the corrected planting with first `Iab` omission order

```text
p,f1,...,fd,f0
```

and upper transitions `{0,2,4,6,8,10}`.  In the label-attached `Ica` block,
swap `f_s,f_(s+1)`, where `1<=s<=d-1`.  Put `x=f_s`, `y=f_(s+1)`.  Relative
to the untwisted planted packet, the exact extra lower action is

\[
\begin{aligned}
R_q^{(s)}={}&{f1}_{q=s+1}
 Q_{K\infty c\,\{f_{s+2},\ldots,f_d\}}(a,p;x,y)\\
&+{f1}_{q=d+1-s}
 Q_{K\infty c\,\{f_1,\ldots,f_{s-1}\}}(a,f_0;x,y)\\
&+\sum_{h=1}^{s-1}{\bf1}_{q=d+1-s+h}
 Q_{K\infty\,\{f_h,\ldots,f_{s-1}\}}(a,c;x,y),
\end{aligned}
\tag{1.1}
\]

where

\[
 Q_R(u,v;x,y)=e_{Rux}+e_{Rvy}-e_{Rvx}-e_{Ruy}.
\]

Now replace only the extreme filler label `p` by a fresh `p'` in a second
copy and subtract the two complete signed packet actions.  The common-order
action contains no `p`; in (1.1), only the first term contains `p`.
Therefore every depth cancels except `q=s+1`, where the result is

\[
 Q_{K\infty c\,\{f_{s+2},\ldots,f_d\}}
       (p',p;f_s,f_{s+1}).                                  \tag{1.2}
\]

This is one primitive Johnson square with coefficient one.  Taking
`s=q-1` isolates an arbitrary square at every depth.  Coordinate relabelling
realizes every prescribed common core and four outside labels under the
stated slack hypothesis.  Johnson squares generate
`ker_Z(partial_(r-q))` integrally, so (1.2) proves (0.1) depth by depth with
no index or parity loss.

An independent second proof uses the triangular adjacent-twist reflection
matrix plus the fact that three-cubes generate the kernel of pair incidence.
It reaches the same integral product and separately verifies that the even
middle layer has no factor-two obstruction.

## 2. Exact physical limitation

The two copies in (1.2) are formal signed catalogue columns, not disjoint
slots.  Every planted upper screen omits both extreme filler roles `f0,p`.
After replacing `p` by `p'`, those screen owners are therefore identical.
The coatom which omits the changed extreme also collides.  Consequently the
two packet words cannot be co-planted as owner-disjoint intervals.

The formal span can become a serial physical theorem only after one proves
one of:

1. a regenerative path from the `p` slot to the `p'` slot;
2. a resource-disjoint circulation cancelling the shared owners; or
3. a different representative of the same pure square with private owner
   and boundary resources.

Nonnegative applicability and occurrence-address compatibility remain
additional requirements even after such a lift.

## 3. The breaker is compiler-local but not compiler-monotone

The single adjacent swap changes exactly `2d+2` complete marginal
compiler-cell signatures in each phase.  If `m` is the first swapped owner
position, the changed zero-based width/start pairs are

\[
\begin{aligned}
 \mathcal D_1(m)&=\{m-1,m,m+d+1,m+d+2\},\\
 \mathcal D_h(m)&=\{m,m+d+2-h\},\qquad 2\le h\le d.
\end{aligned}                                                \tag{3.1}
\]

For the two planted phases,

\[
 m_{\rm old}=6d+19,\qquad m_{\rm new}=5d+16.               \tag{3.2}
\]

Locality does not imply preservation.  Let `M` be an old optimal compiler
matching.  For a legal new cap state `theta`, retain the edges of `M` still
present, let `ell(theta)` be the number lost, and let `alpha(theta)` be the
maximum number of pairwise vertex-disjoint augmenting paths from the
retained matching.  If `lambda` is optimized common-cap deficiency, then

\[
 \boxed{
 \lambda(T^+)-\lambda(T^-)
   =\min_{\theta\in\Theta(T^+)}
       \bigl(\ell(\theta)-\alpha(\theta)\bigr).}
 \tag{3.3}
\]

Thus preservation is equivalent to one legal new state with
`alpha>=ell`, and strict improvement to one with `alpha>ell`.

The condition cannot be inferred from (0.1).  A literal two-target,
two-cell example has marginal incidence graph `K_(2,2)`, hence both perfect
matchings and the full Pluecker square, but one protected common-cap union
excludes both perfect matchings.  Its marginal matching rank is two and its
exact common-cap rank is one.  This is shore-minimal.

## 4. Correction to the previous H2 obstruction

The mass-two isolated flag fibre in
`MATH_THEOREM_H2_COATOM_FLAG_NONNEGATIVE_MARKOV_OBSTRUCTION_20260801.md`
remains correct for the common-order short-flag subcatalogue.  It is not a
no-go for the enlarged endpoint-planted catalogue: the one-block reorder is
outside its move set and breaks the reflected current without requiring the
catalyst used there.

Accordingly the current exact hierarchy is:

```text
formal integral all-depth span              CLOSED
common-order nonnegative Markov property    FALSE
physical pure-depth isolator                OPEN (owner collision)
terminal common-cap preservation            iff alpha >= ell
bounded regenerative compiler               OPEN
```

No `B(k)+O(1)` conclusion follows.

## 5. Replay

Run

```text
python3 scratch/audit_h2_coatom_internal_twist_pure_depth_span_20260801.py
python3 scratch/audit_coatom_order_breaker_compiler_footprint_and_linkage_20260801.py
```

The first replay checks the exact all-depth formula (1.1), pure-depth
cancellation (1.2), and small uniform-layer square ranks.  It reports

```text
PASS_COATOM_INTERNAL_TWIST_PURE_DEPTH_FULL_PRODUCT_SPAN
```

with canonical payload

```text
c92ca485cb3a2e20893d9e41eb17bc0ba04098985b0d11aaf33842b998c6c6fb
```

The second replay checks (3.1) in both phases for `2<=d<=32` and exhausts
the shore-minimal common-cap counterexample.  It reports

```text
PASS_COATOM_ORDER_BREAKER_COMPILER_FOOTPRINT_AND_LINKAGE
```

with canonical payload

```text
3d9d14fa6b9a2f769f21e09917b0f988bb364eb31eb405cf8f3be9fb52de6b1c
```
