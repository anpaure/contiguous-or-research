You are working on the following extremal set-system problem.  A word

    A=(A_1,...,A_n),   emptyset != A_i subseteq [k],

is universal if every nonempty S subseteq [k] is the union of a contiguous
interval A_i union ... union A_j.  Let nu(k) be the minimum length.  Put

    r = ceil(k/2),
    W = binom(k,r),
    Lambda = sum_{s=1}^{r-1} binom(k,s),
    d = min{t >= 0 : tW + binom(t+1,2) >= Lambda},
    B(k) = W+d.

The lower bound nu(k) >= B(k) is proved, and nu(k)=B(k) is proved for
0 <= k <= 16.  Your objective is an actual unconditional proof that

                         nu(k) <= B(k)+C

for one absolute constant C and every k (or all sufficiently large k, since
the finite prefix can be absorbed into C).  Aim at the whole theorem.  Do not
spend the response merely restating known reductions, proving another scalar
inequality, or giving a generic parity example.

The theorem is the objective, not preservation of the present architecture.
You may replace the rail-queue construction completely if a different
pure-mathematical construction proves the same bound.  If you do so, state
exactly how the new construction supplies every lower, middle, and upper
target and why its additive loss is absolute.  Conversely, if you stay with
rails, exploit their literal structure rather than treating the remaining
gate as an arbitrary hypergraph.  Do not answer merely that the results below
are insufficient: that logical point is already known.  Use them as a
launchpad and attempt the missing construction.

Work from the current repository and read these files completely before
proposing a proof:

  RESEARCH_INDEX.md
  MATH_THEOREM_UNIFIED_RAIL_QUEUE_FULL_FRACTIONAL_SHADOW_BRAID_20260807.md
  MATH_THEOREM_BOOLEAN_PROPER_SHADOW_ENDPOINT_AND_ARBITRARY_FERRERS_BOUNDARY_20260807.md
  MATH_SYNTHESIS_AUG12_INTEGRAL_LATTICE_REBASE_20260812.md
  MATH_THEOREM_PURE_RAIL_FULL_OWNER_LATTICE_AND_DESIGNS_II_DIAGONAL_BARRIER_20260812.md
  MATH_THEOREM_RAIL_PACKET_OPTIONAL_MARKS_KILL_LOWER_TORSION_20260812.md
  MATH_THEOREM_PURE_RAIL_ONE_OWNER_SIGNED_MACRO_AND_CONFORMAL_ABSORBER_GATE_20260812.md
  MATH_THEOREM_CENTRAL_ODD_CYCLES_WREATHS_AND_FIXED_CORE_RAIL_SEMIGROUP_BOUNDARY_20260812.md
  MATH_THEOREM_COMMON_MATE_C8_ALLWIDTH_COLLAR_SPANNING_PHASED_HOST_20260812.md
  MATH_OBSTRUCTION_AND_REDUCTION_ALLWIDTH_C8_RESIDENCE_MNW_CAP_COINSTANTIATION_20260813.md
  MATH_THEOREM_REGULAR_INCIDENCE_FACTOR_PRIVATE_PORT_ROUTER_20260803.md
  MATH_THEOREM_TERMINAL_COMMON_CAP_TWO_CROSS_RAY_RADO_GAMMOID_V2_20260803.md

If you cannot access the repository directly, treat the detailed theorem
statements below as authoritative and ask only for a genuinely indispensable
missing definition.  Do not replace the mathematical attack with a complaint
that the files are unavailable.

You may use every proved result in those files as a black box, but you must
respect its exact quantifiers and projection.  In particular, the following
facts are available.

1. The Aug. 7 unified rail-queue theorem supplies one literal component
   family whose symmetric fractional mixture simultaneously has the exact
   middle-owner and residual-lower marginals, every upper rank, both q1
   palettes, depth-d residence, and simple Johnson/local interval decks.
   The arbitrary Ferrers-boundary theorem closes every scalar containment
   capacity cut.  These are fractional facts, not integral rounding.

2. Put q=d+1, c=R-q and M=k-c for the owner rank R used by a pure rail.  A
   closed pure rail has named-owner column

       f(C,T,sigma) = sum_{I in I_q(sigma)} e_{C union I},

   where C is its core and I_q(sigma) is the cyclic q-window deck on its
   toggle order.  The named-owner columns of all legal nonmaximal pure rails
   generate the full signed lattice Z^{binom([k],R)}.  Optional lower marks
   (and genuinely optional upper witness marks) split off by literal unit
   directions.  Every whole closed rail has trace boundary zero.  Therefore
   generic determinant-two boxes, open-trace parity, and optional-mark
   torsion are already irrelevant.  This says nothing by itself about the
   nonnegative, owner-disjoint semigroup after compulsory rows are retained.

3. A fixed-core positive factorization is impossible in general.  Every
   fixed-core rail contributes toggle degree divisible by q, so it would
   require

       q | binom(M-1,q-1).

   At k=17, q=4 and M=12, but 4 does not divide 165.  Thus the positive
   construction must allocate owners across overlapping cores.  A fixed-core
   Baranyai/wreath theorem cannot finish the proof.

4. For every named owner H=C union P with |P|=q and x in P, the Aug. 12
   signed-macro theorem gives a conformal current Y_H with

       P_R Y_H = 1_H,
       Z_H := e_H-Y_H = B^+ - B^-.

   Here B^+,B^- are simple equal-size owner matchings with the same point
   degrees.  This is not an arbitrary degree-fibre pair.  Its structure is:

   - Delta_x is an insertion current on the core C, and H is its unique
     designated positive owner.
   - Choose a (c+1)-set S disjoint from C and containing P, choose
     z outside C union S, and put E=[k]\(S union {z}), |E|=M-2.
   - For u in S,

       Phi_u(sigma)
         = f(S\{u}, E union {z}, sigma^+)
           - f(S\{u}, E, sigma),

       P_R Phi_u = 1_S-e_u+q e_z.

   - The correction star is

       K = sum_{p in P\{x}} (Phi_x^{(p)}-Phi_p),

     and its cores are exactly the star cores S\{u}; these are disjoint from
     the insertion core C.  The centre copies can be packed within each sign,
     different leaves are separated by the missing label u, and H appears
     only positively.  With Y_H=Delta_x+K, removing H leaves the special
     residual B^+,B^- above.

   Keep the signs straight: if Y_H=Y_H^+-Y_H^- and H occurs only in Y_H^+,
   then

       B^+ = Y_H^-,       B^- = Y_H^+ - e_H,

   so Z_H=B^+-B^-.

5. Do not invoke generic 2-switch connectivity for B^+,B^-.  Uniform
   hypergraph degree fibres can require large trades.  The needed proof must
   exploit the insertion/core-star geometry above.  A particularly useful
   positive target is a common-reserve identity: construct an owner matching
   R_H and internally owner-disjoint closed-rail collections Q_H^+,Q_H^- such
   that

       R_H disjoint_union B^+ = disjoint_union_{Q in Q_H^+} supp f(Q),
       R_H disjoint_union B^- = disjoint_union_{Q in Q_H^-} supp f(Q).

   This would lift Z_H conformally.  A bounded terminal error is allowed, but
   it must be absolute, not O(d), O(k), or Catalan-scale.  You may replace this
   reserve formulation by an equivalent compound closed-rail absorber, but
   prove exact owner-disjointness, signs, and compulsory ticket preservation.

6. The common-mate C8 work proves an all-width-zero odd socket actuator and
   embeds its protected bank in a spanning ordered two-SDR.  However the
   current collar is NOT resident: the label z_1 has an immutable internal
   run of length 2.  Any depth-d resident replacement must have variable
   aperture at least d+1.  The Aug. 13 theorem says that any redesigned
   properly phased closed bank with e=2^{o(m)} and lower/upper exposures
   alpha,beta=o(m) automatically extends to a spanning phased host.  Thus you
   need a resident aperture-(d+1) replacement retaining the all-width
   prefix/full-union identity, inherited socket action

       theta_1 = theta_0 o s,

   and bounded-component fusion.  Do not claim that the existing two-slot
   collar is resident or that adding endpoint guards repairs its internal
   run-two defect.

7. A common-cap proof is occurrence-level.  It requires pairwise-private
   literal claim-to-port prefixes and, after all used capacity is deleted,
   one simultaneous typed suffix linkage of the active occurrence-labelled
   port set P to distinct sinks in the same cap/phase/guard state.  Its exact
   criterion is

       r_{Gamma_suf^type}(P)=|P|

   (equivalently every node-split cut from S subseteq P has capacity at least
   |S|).  A spanning factor or ordinary Hall marginal does not imply this.

The proof should be organized around one coherent protected integral
rail-queue theorem, not around independent marginal choices.  Establish the
following four gates in one occurrence-labelled construction.

Before settling for a partial result, seriously attempt at least these three
whole-proof mechanisms and explain which exact equation succeeds or fails:

  (i) a common-reserve/compound absorber telescoping the explicit star-core
      residual B^+-B^-;
  (ii) a global overlapping-core allocation followed by variable-period
       cyclic deck decomposition, using cross-core role flow to cancel the
       fixed-core q-divisibility; and
  (iii) a replacement global construction (for example a symmetric-chain,
        Hamilton-decomposition, integer-flow, or iterative-absorption
        architecture) that bypasses the rail semigroup but still retains the
        exact B(k)+O(1) accounting.

Prioritize a mechanism that closes several gates simultaneously.  A lemma is
valuable here only if you immediately propagate it through the proof spine or
show that it is the unique remaining condition.

GATE A: OVERLAPPING-CORE POSITIVE OWNER SEMIGROUP.

Starting from the fractional rail-queue mixture, produce an owner-disjoint
nonnegative selection of literal closed rails covering every named owner,
apart from at most C_0 owners.  The selection must carry the optimal Ferrers
lower flags and balanced closed trace states.  Prove this with an absorber,
integer-decomposition theorem, randomized nibble plus deterministic reserve,
or another explicit mechanism.  The central local task is the conformal
closed-rail lift of the special B^+,B^- pair.  Try first to exploit telescoping
among the star cores S\{u}, the common exterior E, the inserted z, and the
disjoint insertion core C.  In particular, compute whether suitably coupled
orders make the residual decks cancel into whole rails or into a bounded
family of compound cycles.  If a reserve is used, prove it survives repeated
absorptions and that its loss does not grow with k.

Signed lattice membership is not enough.  State and prove the exact positive
semigroup or bounded-hole statement you use.  Every aggregate positive and
negative shore must be a simple owner matching, and every compulsory row must
be accounted for.  Optional marks may be chosen after the physical columns,
but compulsory palettes, residence, phase/socket, topology, nonoptional upper
witnesses and typed-cap resources may not be projected away.

GATE B: LOWER FLAGS, RESIDENCE, ALL-WIDTH UPPER WITNESSES, AND TOPOLOGY.

Show how the integral owner selection receives one nested Ferrers flag per
owner, hence actual occurrence-labelled lower targets at every depth, while
preserving depth-d residence.  Coinstantiate a resident aperture-(d+1)
all-width odd-socket bank with the surrounding rail factor.  Use the proved
low-exposure protected-host theorem once the bank is constructed, but also
prove that the surrounding ordered two-SDR has only O(1) Euler components (or
give explicit protected switches that fuse all but O(1)), and that every
upper target keeps a literal witness through the fusion.  The phase/socket
action must agree with the inherited recursive terminal labels, not merely up
to an unnamed abstract cycle.

GATE C: LITERAL COMMON CAP IN THE SAME STATE.

Plant the pairwise-private claim-to-port prefixes during Gates A-B, not after
the factor is frozen.  Build the typed suffix router, or prove its all-cut
condition, after deleting every prefix interior, compensation path, factor
resource and protected witness resource actually used.  All paths must be
pairwise disjoint, correctly typed, and live in one fixed occurrence/cap/phase
state.  Explain why the positive owner absorber and the topology fusion do not
consume this router reserve.

GATE D: REGENERATION AND THE WORD.

Either construct the object directly for every sufficiently large k, or give
a same-parity k-2 -> k induction whose exported state has bounded size and is
identical in kind to its input state.  Prove that owner, lower, upper,
residence, topology, socket, and cap errors do not accumulate.  Then invoke
the appropriate Shadow--Braid/factor-to-word theorem (with exact hypotheses
quoted) to obtain a literal universal OR word of length B(k)+C.  Give an
explicit absolute C, even a very large one, and explain how the finitely many
small k are absorbed.

Do not use any of the following invalid shortcuts:

  * fractional feasibility => integral feasibility;
  * signed full lattice => nonnegative semigroup normality;
  * generic parity or Smith-normal-form examples not embedded in the actual
    complete closed-rail columns;
  * Keevash/Designs II without checking growing molecule size, extendability,
    divisibility in every compulsory row, and bounded leave;
  * fixed-core factorization despite q not dividing binom(M-1,q-1);
  * generic 2-switch connectivity of uniform hypergraph degree fibres;
  * separate owner, palette, topology, residence, upper, and cap matchings
    whose occurrence labels are never recoupled;
  * treating an optional witness mark as optional when it consumes a
    compulsory resource;
  * using the current two-slot all-width C8 collar as though it were resident;
  * an O(d), O(sqrt(k)), o(W), or Catalan-size terminal leave.  The theorem
    needs an absolute O(1) leave.

Proof standard and requested output:

1. Lead with a precise theorem implying nu(k) <= B(k)+C.
2. Give the literal construction, not just a list of desired properties.
3. Prove each gate with exact ranges, constants, signs, disjointness, and
   quantifiers.  Separate a signed identity from a positive decomposition.
4. Audit the special B^+,B^- residual explicitly: list every core family,
   owner multiplicity, point degree, and any compulsory ticket current before
   claiming a compound absorber.
5. Audit the final object occurrence-by-occurrence against the hypotheses of
   the factor-to-word theorem.
6. Do no computational search.  Small examples may sanity-check formulas but
   cannot be premises.

If, after a serious attempt, a full proof fails, do not fill the answer with
minor lemmas.  Instead give the strongest decisive result on the actual live
gate: either (i) a conformal reserve/compound lift for the special B^+,B^-
pair, (ii) a resident aperture-(d+1) all-width socket bank that coexists with
the rail absorber, or (iii) the private-prefix typed-router theorem in that
same bank.  If you claim an obstruction, embed it as an explicit integer
character, separating cut, or positive-semigroup hole of the ACTUAL complete
closed-rail columns, including compulsory coordinates; a generic model
counterexample is not responsive.
