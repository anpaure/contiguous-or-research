# Constant one after the sixteen-lane audit: corrected live gates

Date: 2026-07-26

Method: pure mathematics.  This note records only implications and local
objects which have survived the exact-ownership, geodesicity, monodromy,
carrier, and rounding audits.  It does **not** claim MWB or coefficient one.

## 0. Verdict

The current work has converged to one exact consumer and three genuinely
different construction gates. Two constructions which previously looked
live have now been closed in their literal forms: the completed paired
moving-exterior `C_6` is all-depth histogram-neutral, and the disjoint-slot
reciprocal `C_8` atlas is trapped inside coordinate-orbit mass classes.

The consumer is the critical root-scale hinge theorem `CRH_A` from
`MATH_SYNTHESIS_W_CRITICAL_PAIR_HINGE_GATE_20260726.md`.  For every fixed
`A`, it asks for one full-overlay component law whose mean quota excess and
targetwise fluctuation remainder satisfy

\[
                 \Phi_\beta(\mathbb P)+\mathcal R_\beta(\mathbb P)=o_A(W).
\tag{0.1}
\]

Every state in the support is then already an integral exact factor, and
the zero-margin rounding theorem gives fixed-window MWB.  Diagonalization
and the uniform product-SCD tail give

\[
 \mathrm{CRH}_A\ (\forall A)\Longrightarrow \mathrm{MWB}
 \Longrightarrow \nu(k)=(1+o(1))W(k).
\tag{0.2}
\]

No current pair proves (0.1).  The three live ways of trying to produce it
are:

1. a **non-coordinate** bounded-component carrier packet;
2. a linear-support return-router/Fano atlas whose completion is not
   all-depth neutral;
3. a multiscale MSW four-arm trade cube with critical saturation and
   cross-parent rounding.

All fixed-exterior twisted conveyors, diagonal product-voltage lifts, and
one-arm max-flow reductions are now rigorously closed.

## 1. The first literal moving-exterior router

`MATH_THEOREM_SMALLEST_MOVING_EXTERIOR_C6_PACKET_20260726.md` constructs a
literal packet with

\[
                         (h,r,e)=(3,2,1).
\tag{1.1}
\]

For `|C|=m-3`, exteriors `C+u,C+v`, and ports

\[
                         P_i=\{k,a_i\},\qquad i\in\mathbb Z_3,
\tag{1.2}
\]

its two settings are three vertex-disjoint length-two Johnson geodesics
with literally identical complete `X/Y` ledgers.  Their absolute twists
are `tau^2` and `tau`, and both satisfy

\[
 |(C+u)\setminus(C+v)|
 =|P_i\setminus\tau P_i|
 =|P_i\setminus\tau^2P_i|=1.
\tag{1.3}
\]

Thus the abstract carrier-module direction is physically nonempty.  This
is the first nontrivial router in the present audit which survives the
moving-exterior metric equation and the complete collar ledger.

Its output boundary is a complement triangle while its input is a star.
Direct repetition is therefore impossible by a coordinate relabelling.
The triangle can nevertheless be closed by a second terminal `C_6`.
Append fresh-hub uppers

\[
 Z_i=C\cup\{v,k',a_i,a_{i+1}\},
 \qquad B_i=C\cup\{v,k',a_i\}.
\tag{1.4}
\]

Opposite choices of the entrance and terminal `C_6` give two literal
length-three star-to-star settings with absolute twists `tau` and
`tau^{-1}` and identical complete vertex ledgers.  Their exchange lists
are

\[
 (k\mapsto a_{i+1},u\mapsto v,a_i\mapsto k'),
 \qquad
 (k\mapsto a_{i-1},u\mapsto v,a_i\mapsto k'),
\tag{1.5}
\]

so length equals endpoint distance in both settings.  This seam is
minimal.  There is also a sharp no-repeat theorem: its unique output
petal was inserted in the packet, whereas any following nonidentity
star-to-star router must remove that petal.  Direct repetition therefore
backtracks, independently of the next hub.  The next physical target is a
multi-petal storage port or one larger norm-closing packet, not another
copy of the same seam.

The literal rowwise completion of the paired packet has also been audited.
For its two three-row settings `Phi_i,Psi_i`, the complete cyclic interval
histograms agree at every length:

\[
 \sum_{i,h}[I_{\Phi_i}(h,t)]
 =\sum_{i,h}[I_{\Psi_i}(h,t)]\qquad(1\le t<n).
\tag{1.6}
\]

Consequently every MWB carrier is exactly zero after completion. Leaving
the packet open retains nonidentity monodromy at positive density, so this
particular packet cannot be the coefficient-one engine.

There is also a statewise monotonicity theorem for concatenated common-core
`C_6` routers. Once a petal is transferred from one strand to another, a
global Johnson geodesic can never transfer the same strand pair again.
Thus the router supports form a linear 3-uniform hypergraph. Repeated,
inverse, and tetrahedral supports are excluded at every storage rank. The
seven lines of the Fano plane are the first algebraically viable closure;
their twists admit an identity product, but a literal fresh-`X/Y`, nonneutral
Fano atlas is still open.

## 2. A large abstract `C_6` supply and the exact physical gap

The incidence-hexagon hypergraph on

\[
 \{K+a,K+b,K+c;K+ab,K+bc,K+ca\}
\tag{2.1}
\]

has asymptotic degree `r^3/2`, maximum pair codegree `r(r-1)`, and a
matching covering all but `o(rC_r)` vertices on both shores.  Thus it
contains `(1/3-o(1))rC_r` vertex-disjoint geometric hexagons.

Relative to an actual outgoing matching, however, alternating hexagons
are directed triangles in the core functional digraphs.  Matching density
alone does not force them; there is a half-shore matching with none.  For
a fully upper-saturating port matching the total triangle count remains a
global cross-core question.

Closed inverse or tetrahedral `C_6` tubes have now been written with
identity monodromy, literal complement geodesics, and complete collars.
They require about `C_r/4` return routers.  The old ballot theorem supplies
only `C_r/r`, whereas the geometric hexagon near-decomposition supplies
more than the required raw count.  The live theorem is therefore:

> orient and phase the near-decomposition so that its routers satisfy the
> return-router Hall system, strand compatibility, and the frozen Catalan
> corridor simultaneously.

This is a real density-matching gate, not a missing integrality lemma.

## 3. The reciprocal `C_8` atlas

The canonical first-two-cut reciprocal rectangles form exactly
`C_{s-2}` two-root full-overlay components.  Their exact edit moment is

\[
 \Xi_{\rm can}=8{C_{s-2}\over C_s}\longrightarrow {1\over2},
\tag{3.1}
\]

they change `(1/8+o(1))C_s` roots, and they transfer
`(1/16+o(1))C_s` first-boundary units.  Hence a coherent bank of four to
seven such layers would have `Xi=O(1)` and enough raw scalar capacity for
the hard parent demand.

The combinatorial atlas is now constructed.  Choose a constant number of
pairwise disjoint literal word slots and use the partial involutions

\[
                  U1100V\longleftrightarrow U1010V.
\tag{3.2}
\]

The substitutions preserve one another's eligibility and their complete
chronology slabs are disjoint.  Hence every subset of the layers is a
literal anchored exact factor.  The full ownership components are exactly
the root cubes and have size at most `2^u`.  Their exact edit moment is

\[
 \Xi_u={4\over C_s}\sum_{h=1}^u\binom uh h2^hC_{s-2h}
 \longrightarrow {u\over2}\left({9\over8}\right)^{u-1}.
\tag{3.3}
\]

Every fixed `u` therefore passes the sparse-edit threshold.  The marked
inventory is exactly `uC_(s-2)`, so `u=4,...,7` covers the complete scalar
demand range.  This cubical construction uses overlapping root supports;
the smaller `C_(s-1)` ceiling for root-disjoint contextual packets does
not apply.

The physical sign audit is negative for this whole coordinate-layer class.
If

\[
 H_s=\langle(2\ 3),(4\ 5),\ldots,(2s-2\ 2s-1)\rangle,
\]

then every complete layer column has zero sum on every `H_s`-orbit of
physical targets, at every protected rank. The identity survives all
mixed-window interactions. In particular the fatal parent pair retains
total mass `C_s+C_(s-1)`, forcing an `Omega(C_s)` cap-excess term. Thus
four to seven disjoint shifted layers do **not** have a common favourable
untagged PCap orientation. A successor must contain a genuinely
non-coordinate packet which crosses these target orbits.

By contrast, the clean suffix-`C_8` bank is closed.  Its strand
displacements are incompatible, and any bounded-congestion collar
amplifier has only `O(s)` full carrier action, versus the required
`Omega(s^{3/2})`.  A successful clean-`C_8` escape would require
`Omega(sqrt(s))` congestion and must still pass the sparse-edit moment.

## 4. The four-arm trade cube

At a fixed depth an MSW rectangle bit has signed column

\[
 a_e=\mathbf1_{P_e}-\mathbf1_{N_e},
 \qquad |P_e|=|N_e|=4.
\tag{4.1}
\]

The four Johnson arcs are ganged.  The matrix is not an ordinary network
matrix: a three-column triangle has a determinant-two minor and

\[
                      K_1^{\mathbb Z}=2,
 \qquad                K_1^{\rm LP}=3/2.
\tag{4.2}
\]

The exact fractional dual is the weighted-potential formula

\[
 \max_{0\le\alpha\le1}
 \left\{
  \langle\alpha,\mu-b\mathbf1\rangle
  -\sum_e\bigl(\alpha(N_e)-\alpha(P_e)\bigr)_+
 \right\}.
\tag{4.3}
\]

Indicator cuts are not sufficient.

Nevertheless, integrality is cheap at one depth and on logarithmic
windows.  For `M` rectangle bits, independent rounding costs at most
`2M` at one additive depth.  For the full nonadditive cube, boundary
locality gives an exact state with loss at most `16M|Q|`.  At the fatal
Catalan scale

\[
                         M=\Theta(W/r^{3/2}),
 \qquad r=\Theta(\log p),
\tag{4.4}
\]

this is `o(W)` for `|Q|=O(log p)`.

Inside one aligned parent packet the vertically stacked multidepth matrix
is in fact totally unimodular.  Every non-TU obstruction must cross parent
contexts.  If

\[
 \Lambda_{\mathcal Q}
 =\sum_{q,S}w_q\left(d(q,S)-\max_Cd_C(q,S)\right),
\tag{4.5}
\]

then the additive rounding loss is at most `2 Lambda_Q`.  The Gaussian
rounding gate is therefore the concrete Catalan collision estimate

\[
                         \Lambda_{\mathcal Q}=o(W),
\tag{4.6}
\]

or its state-dependent full-cube analogue.

There is a second route to remove the Gaussian depth factor.  Put

\[
 D_q=K_q(\mu_q^0)-c_q.
\tag{4.7}

If `D_q>=4M-delta_q` throughout the window and

\[
                         \sum_q\delta_q=o(W),
\tag{4.8}

then every low-PCap fractional mean is forced near the all-on corner, and
the all-on exact factor has aggregate residual at most twice the mean plus
`sum delta_q`.  Thus (4.8) solves integrality on an arbitrary window.  The
present one-scale constants leave an error of order `M/r` per depth, so a
multiscale saturation audit is necessary.

That scalar audit is now positive when two adjacent Catalan scales are
allowed.  With

\[
 M_r=H_{m,r+1}C_{r-1},\qquad
 D_r=H_{m,r}(C_r/2-p),
\tag{4.9}
\]

and `r` minimal with `C_r>=4p`, one has

\[
 {D_r\over4M_r}<{7\over4},\qquad
 {M_{r+1}\over M_r}>{3\over4}\quad(r\ge5).
\tag{4.10}
\]

Hence `M_r+M_{r+1}>D_r/4` uniformly over the entire Catalan overshoot
interval.  For `Q=[r,H]`, choosing

\[
 L=\left\lfloor{D_r-(W-N_H)\over4}\right\rfloor
\tag{4.11}
\]

makes the tuned scalar criticality deficit vanish at every `q in Q`.
The cross-scale physical audit is also positive.  The unique incompatible
local chain is

\[
 110010S\longleftrightarrow101010S
             \longleftrightarrow101100S.
\tag{4.12}
\]

All such conflicts form a matching.  Delete one endpoint from each
conflict.  Every remaining affected phase-edge slab is disjoint, so the
surviving catalogue is a literal joint Boolean cube with complete `X/Y`
ownership, equal path lengths, and exact ports.  Its size is

\[
 C_r=M_r+M_{r+1}-H_{m,r+2}C_{r-1},
\tag{4.13}
\]

and an exact rational calculation gives `C_r>D_r/4` throughout the full
overshoot interval.  Hence the physical cube can still be cardinality
tuned so that the scalar saturation defect is zero on the whole
`p^(1/4)` plateau window.

Thus Catalan arithmetic, Gaussian rounding, and cross-scale compatibility
are no longer the obstruction.  The sole remaining gate in this lane is
directed: prove a common multidepth four-arm fractional/dynamic drain on
the tuned cube.  Scalar capacity does not say that the four removed
targets are overloaded while the four inserted targets have slack.

The first directed test is negative.  At the matched depth `q=r`, take
the common dual weight `alpha=1_(Omega_r)`, where `Omega_r` is the
canonical overloaded set.  Every scale-`r` bit has drain at most two,
because two marked arms stay entirely inside `Omega_r`; a retained
scale-`r+1` bit has the universal bound four.  Therefore every integral
cube state and every probability mixture satisfies

\[
 K_{r,p}\ge
 H_{m,r}(C_r/2-p)-2M_r-4\widetilde M_{r+1}.
\tag{4.14}
\]

The normalized right side is positive when
`C_r/p>16/3+o(1)` and approaches `(1/8)H_(m,r)C_r` near the worst
overshoot.  This does not yet refute `PCap=o(W)`, because one such depth
is only `Theta(W/r^(3/2))=o(W)`.  The decisive audit is whether a coherent
family of these protected-set dual witnesses persists through
`Theta(p^(1/4))` depths; persistence would give an aggregate obstruction
much larger than `W`.

That persistence audit is now sharp enough to give a partial no-go, but
not a uniform closure. For every retained scale-`r` or scale-`r+1` bit and
every `q>=r+1`, one hereditary suffix arm has both endpoints already above
cap, so the canonical overloaded-set dual credits at most three isolated
units per bit. Double-boundary rescues have fixed-depth count
`O(W/r^3)`. Applied to the **full** compatible two-scale cube, this gives
an aggregate obstruction only when

\[
                    {C_r\over p}>{64\over11}+o(1).
\tag{4.15}
\]

The earlier `16/3` threshold came from a deliberately tuned subcube and
cannot close the larger legal cube: adding exact variables may help. For
`4<=C_r/p<=64/11+o(1)` the present dual is inconclusive. The preceding
scale `r-1`, whose Catalan loads straddle the cap, is a plausible source
of the missing directed arm. The current decisive audit is therefore the
literal `r-1,r,r+1` conflict cube and its full weighted-potential drain.

## 5. Closed routes

The following may still be useful as local ingredients, but they cannot
be the global construction in their present form.

1. **Fixed exterior.**  A nonidentity twist violates
   `e=|P\setminus tau(P)|`.
2. **Diagonal product voltage.**  Exact slotwise ownership forces every
   coordinate generator into the acting subgroup, hence restores the
   full exponential product component.  Phase gauges do not help.
3. **One-arm flow.**  It ignores the four-arm synchronization and its
   Hall dual is false for the physical trade matrix.
4. **Abstract ballot cycles.**  A functional cycle need not alternate in
   the full factor and does not by itself give equal-length strands.
5. **Separated growing parents.**  Their occurrence supply is
   `O(C_r/sqrt(s))`, below the required Catalan density.
6. **The all-block conjugate pair.**  For
   `G_s=eta_s F_s(eta_s cdot)`, every equal adjacent Dyck block forces a
   distinct Kendall inversion.  Its exact mean number is
   \[
     {(s-1)(s-2)\over2s-1}\sim{s\over2},
   \]
   so `Xi(F_s,G_s)=Omega(s)` independently of overlay fragmentation.
   This closes it as a sparse-edit `CRH_A` pair.
   In fact
   \[
   \operatorname {Aut}(\mathcal D_s)
   =\langle(2i\;2i+1):1\le i<s\rangle\cong C_2^{s-1},
   \]
   because the consecutive coordinate-degree gaps are
   `C_i C_(s-i-1)>0`.  Hence this grammar exhausts every covariant
   coordinate conjugate of the canonical seed.  Any better seed must be
   context-dependent or trade-built.

## 6. Current decisive questions

The following four statements are now the shortest nonredundant targets.

1. **Non-coordinate carrier atlas:** construct a bounded-component exact
   packet crossing the even-pair target orbits, with one favourable raw
   multidepth carrier. The tagged `D_3`-pentagon/`D_4` pair transfer is a
   coefficient-one decorated prototype, but its simple product embedding
   has a rank-four raw-mask separator.
2. **Return-router theorem:** extract `Omega(C_r)` phase-compatible
   alternating hexagons from an exact upper-saturating port matching and
   close them into a linear-support Fano or other nonneutral tube atlas.
3. **Three-scale four-arm drain theorem:** on the literal compatible
   `r-1,r,r+1` cube, prove a product mean with aggregate `o(W)` PCap, or
   exhibit a coherent weighted-potential obstruction throughout the full
   Catalan overshoot interval.
4. **Parent-aligned raw carrier:** lift the literal positive-density
   `D_3`-pentagon/`D_4` factor trade so its pair-class coefficient-one
   carrier becomes the same undecorated rank target, while retaining every
   crossing collar and a favourable nonlinear cap sign.

Any one of these still needs a directed mean-hinge calculation before it
can prove (0.1).  The first three are currently the closest because their
raw Catalan supply is already at the correct order.
