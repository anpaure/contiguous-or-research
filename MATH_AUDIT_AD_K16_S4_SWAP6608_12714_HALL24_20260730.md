# AD audit: the S4 length-three swap gives Hall 24 but loses upper `4e79`

Date: 2026-07-30  
Status: **exact middle and Hall audit PASS; unrestricted upper completeness
FAILS in exactly one mask**

## 1. Authenticated words and literal operation

The frozen base and candidate are

```text
cae23cfcedbc9d193ebd9191045edd0e5c14f56af5fc96a802b8cefa7cd7e974
  scratch/ad_k16_bad2_splitpair_exact_20260730/exact_229.targets
955dc2fb350cd399f050e4dafb481925633e0024eaa438ee3cf4f984e0acb995
  scratch/s4_exact229_swap6608_12714_l3_20260730.targets
```

The candidate differs in exactly six positions.  It swaps, without reversing,
the two length-three blocks beginning at positions 6608 and 12714:

```text
position 6608:   ce38 cc3c cc2e   ->   4e78 4e3c 4c3e
position 12714:  4e78 4e3c 4c3e   ->   ce38 cc3c cc2e.
```

All positions are zero-based and intervals below are half-open.  The row
multiset is therefore unchanged.  For cell comparison, every compiler cell
is labelled by the ordered tuple of original source-row occurrences occupying
it.  These labels are injective in both words.

## 2. Exact middle geometry and the upper failure

### Theorem 2.1 (literal geometry)

The candidate has length 12873, flat positions

```text
6320 12869 12871,
```

the same depth schedule as exact229, total compiler capacity 32063, no zero
maximal envelope, and exact middle replay at every row.

Its unrestricted upper-hole set is, however,

```text
{4e79}.
```

Exact229 has exactly one literal interval for this upper mask:

\[
 [12713,12715):\qquad 4a79\mathbin{\lor}4e78=4e79.
\]

The swap moves the `4e78` row away from this interval.  A complete
start-by-start interval-OR replay finds no replacement witness in the
candidate.

#### Proof

The replay reconstructs the candidate by the stated block swap and rebuilds
the depth and maximal-envelope arrays from the literal word.  Every row is
then compared with the union of its allowed maximal envelopes.  Separately,
all distinct ORs of intervals ending at each position are enumerated.  This
gives no middle error, no zero envelope, no upper hole for exact229, and the
single candidate upper hole and unique lost base interval stated above.  ∎

Thus the candidate is not an upper-complete carrier, even though its middle
compiler is exact.

## 3. Generalized Hall graph

Let (L) be the 26332 nonempty masks of rank below 8, and let right vertices
be compiler cells canonically labelled by ordered source-row tuples.

| word | incidences | maximum matching | deficiency | canonical shore |
|---|---:|---:|---:|---:|
| exact229 | 347875 | 26307 | 25 | 212 / 187 |
| S4 swap | 347739 | 26308 | 24 | 211 / 187 |

Both canonical shores split into deficiency-one connected components.
Exact229 has 25; the candidate has 24.  The candidate shore is **exactly**
the exact229 shore with target `4e70` removed.  After deleting the isolated
exact229 component

```text
left {4e70}, right {}, deficiency 1,
```

the remaining 24 labelled bipartite components—including every left target,
right source-row tuple, and edge—are identical between the two shores.

The candidate component-shape histogram is

```text
1/0:11, 3/2:2, 4/3:3, 6/5:1, 7/6:1,
15/14:1, 18/17:1, 23/22:1, 34/33:1,
35/34:1, 44/43:1,
```

where every displayed component has deficiency one.

Globally the two compiler graphs share 347623 incidences.  The swap loses
252 and gains 116 incidences on 27 changed canonical right vertices.  The
rank histograms are

```text
lost:   r1:2 r2:16 r3:48 r4:76 r5:69 r6:34 r7:7
gained:      r2:6  r3:22 r4:34 r5:31 r6:18 r7:5.
```

There are 242 distinct targets losing an incidence and 106 gaining one.
These large raw changes are nevertheless irrelevant to the deficiency except
for one alternating component.

## 4. The unique Hall-improving exchange

The fixed maximum-matchings' symmetric difference has 38 components.  Its
histogram `(edge count)/(exact229 edges minus candidate edges)` is

```text
2/0:4, 4/0:12, 6/0:8, 7/-1:1, 8/0:5,
10/0:2, 12/0:2, 14/0:1, 16/0:1, 18/0:1, 20/0:1.
```

Hence there is exactly one unbalanced component.  In path order it is

```text
candidate  4e70 -- (12714,12715)
exact229   4a78 -- (12714,12715)
candidate  4a78 -- (1002,1003,1004)
exact229   4278 -- (1002,1003,1004)
candidate  4278 -- (787,788)
exact229   4038 -- (787,788)
candidate  4038 -- (2725).
```

All edges after the first two are present in both compiler graphs.  The
decisive candidate cell has profile

```text
source rows       (12714,12715)
candidate span    [6608,6610)
ordered envelopes 4e60,4e30
allowed/mandatory 4e70/0a50
```

and therefore hosts `4e70`.  In exact229, the same canonical cell occurs at
`[12714,12716)` with ordered envelopes `4a70,4a38` and profile
`4a78/0258`; it hosts `4a78` and cannot host `4e70`.

### Theorem 4.1 (decisive-provider identity)

In exact229, target `4e70` has degree zero.  In the candidate it has degree
one, with unique provider `(12714,12715)`.  Adding only the incidence

\[
 4e70\sim(12714,12715)
\]

to the exact229 graph raises its maximum matching from 26307 to 26308.
Deleting only this incidence from the candidate graph lowers its maximum
matching from 26308 to 26307.

#### Proof

The degree statements follow directly from the two rebuilt candidate maps.
The displayed seven-edge path is alternating relative to the exact229
matching; its first left endpoint and last right endpoint are unmatched, so
the new edge completes an augmentation.  Direct maximum-matching replay after
the one-edge addition and deletion gives 26308 and 26307, respectively.  The
canonical-shore decomposition independently confirms that precisely the
isolated `4e70` component disappears and all other components remain
unchanged.  ∎

## 5. Exact conclusion and remaining boundary

The length-three swap is a particularly clean local exchange:

```text
Hall side:   supplies unique lower provider 4e70 and changes 25 -> 24;
upper side:  destroys unique interval witness for 4e79 and changes 0 -> 1 hole.
```

This proves a fixed-word Hall/upper trade, not a feasible exact229 successor.
Any use of this Hall24 state must additionally restore a literal interval for
`4e79` while retaining `4e70@(12714,12715)` and the six retained edges of the
augmenting path.  No such compensating braid is proved here.

## 6. Reproducibility

The solver-free independent replay is

```text
scratch/audit_ad_k16_s4_swap6608_12714_hall24_20260730.py
```

and writes

```text
scratch/s4_exact229_swap6608_12714_l3_20260730.audit.json
```

The JSON status is

```text
PASS_MIDDLE_EXACT_HALL24_WITH_ONE_UPPER_HOLE
```

and its internal payload hash is

```text
302190a906d9607920deebf7059a8bea567c535fac54942b6227b2db5dc3d92f
```

The audit performs no SAT solve or chronology search.  Its only exhaustive
steps are exact replay of the two supplied fixed words, their finite compiler
incidence graphs, and deterministic bipartite matching.
