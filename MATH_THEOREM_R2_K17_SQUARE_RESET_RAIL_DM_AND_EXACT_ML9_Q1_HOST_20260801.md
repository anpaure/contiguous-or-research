# `k=17` R2: the square crosses ae88 only as an outer overlay, while the opened reset rail has an exact `ML_9` q1 host

**Date:** 2026-08-01  
**Lane:** R2 / complete-reversal packet, common attachment cuts, exact q1 host  
**Status:** exact incidence theorem, exhaustive scoped ae88 overlay census,
exact physical packet-only `ML_9` f-factor, exact `d`-overlap leave, and
the protected twin-bank rebase.  No global word, rooted Catalan completion,
topology join, upper-complete host, state transversal, terminal compiler, or
regeneration is claimed.

## 0. Outcome

There are four different statements, and they must not be conflated.

1. The user's open three-edge Johnson square is an exact portal path, not a
   closed one-copy circuit.  Restoring its omitted edge gives the unique
   support-four orientation reversal on that square.  In the ae88
   both-live root--owner graph, three clean source-supported closed-square
   overlays have fresh matching number `1142`; explicit matchings are
   frozen.  This is an attachment-graph outer result, not a literal ae88
   flag planting.
2. The authoritative depth-three reset plus long return rail is a genuine
   complete-reversal packet.  On the correct `k=17` attachment layer it is a
   cycle of fourteen rank-nine owners with fourteen rank-eight edge roots.
   Among all clean quotient-simple source-supported ae88 overlays, neither
   a closed packet nor any one-cut open packet crosses every old maximum DM
   shore.  No packet phase equals the fourteen incumbent ae88 flag rows.
   This is a scoped no-go for direct source-supported planting, not for a
   prospectively co-designed host.
3. The generic `m>=8d+4` protected-factor theorem does not apply at
   `k=17,d=3`, because `m=9<28`.  Nevertheless the exact residual f-factor
   problem in `ML_9` is feasible for all fourteen openings.  A canonical
   literal spanning two-factor with `48,620` edges is frozen and independently
   replayed.  Thus the packet's common undirected q1 path has an exact
   physical `k=17` host.
4. The authoritative coefficient-one `d`-overlap supersedes the bare-cut
   loss model.  It restores every packet owner exactly once and every
   lower/middle target.  Its exact remaining leave is two upper Ferrers
   triangles, and explicit `2d`- and `3d`-owner banks duplicate the two
   triangles using `5d` raw protected owner positions and no appended source
   letters.  With the stated four-marker hypothesis and collars, owner/q1
   simplicity, residence and phase commonality are already local theorems.

The older canonical three-cell restitution audit leaves exactly three
forced short targets, with rank histogram

\[
                         10^1,\qquad 11^2.                 \tag{0.1}
\]

Its complete unbanked loss is larger: between `12` and `18` target values,
depending on the cut.  This is retained as a diagnostic only.  For the sharp
`d`-overlap the exact `d=3,r=9` leave is instead the cut-independent family

\[
 10^1,\quad 11^2,\quad 12^3,\quad
 13^1,\quad 14^2,\quad 15^3,                             \tag{0.2}
\]

of twelve upper targets.  The twin-bank target map covers that entire
family under its marker hypothesis.  Hence there is no remaining *local*
ambient-provider Benders row for cut collateral once a valid protected
twin-bank embedding is selected.

There is one exact-`k=17` applicability boundary.  The ae88 attachment layer
has owner rank `r=9`, so the normal form has

\[
 h=r-2d-1=2,
 \qquad |\Omega|=h+4d+2=16,                              \tag{0.3}
\]

with one ambient coordinate unused.  The clean bank-separation theorem
assumes four markers in `H`, and its generic combined protected-factor
corollary assumes `m>=22d=66`.  Neither hypothesis follows for the literal
`ML_9` instance.  Thus the exact packet-only factor in Section 5 does not
silently certify the packet plus both banks.  A marker-tight `k=17` planting,
or another literal resource-separation argument, remains part of the host
gate.

Finally, global reversal is used as an induction gauge.  For a
reversal-closed existential interface one builds one oriented complete child
and reflects its witnesses and compiler.  No fixed-address
`G^+ ∩ G^-` compiler overlap is required.  Reversal does not change
matching deficiency, component count, or cut collateral.

## 1. The exact square vectors

Let `|K|=6` and let `x,y,z,w` be distinct coordinates outside `K`.  Put

\[
 V_0=K+x+z,\quad V_1=K+x+y,\quad
 V_2=K+y+w,\quad V_3=K+z+w,                              \tag{1.1}
\]

and, cyclically,

\[
                         L_i=V_i\cap V_{i+1},\qquad
                         U_i=V_i\cup V_{i+1}.              \tag{1.2}
\]

Explicitly,

\[
\begin{array}{c|c|c}
i&L_i&U_i\\ \hline
0&K+x&K+x+y+z\\
1&K+y&K+x+y+w\\
2&K+w&K+y+z+w\\
3&K+z&K+x+z+w.
\end{array}                                                \tag{1.3}
\]

Use reverse minus forward.  For the open path

\[
                         V_1\to V_2\to V_3\to V_0,        \tag{1.4}
\]

the atom vector is

\[
\begin{aligned}
\Delta_{\rm open}={}&
 +(V_0\to V_3;L_3,U_3)
 +(V_3\to V_2;L_2,U_2)
 +(V_2\to V_1;L_1,U_1)\\
&-(V_1\to V_2;L_1,U_1)
 -(V_2\to V_3;L_2,U_2)
 -(V_3\to V_0;L_3,U_3).
\end{aligned}                                               \tag{1.5}
\]

Its owner and lower-q1 marginals vanish, but its endpoint currents are

\[
 \partial_{\rm tail}=e_{V_0}-e_{V_1},\qquad
 \partial_{\rm head}=e_{V_1}-e_{V_0}.                     \tag{1.6}
\]

On the ae88 head--owner graph its exact vector is

\[
\begin{aligned}
\Delta_{HO}^{\rm open}={}&
 e_{(V_1,U_1)}+e_{(V_2,U_2)}+e_{(V_3,U_3)}\\
&-e_{(V_2,U_1)}-e_{(V_3,U_2)}-e_{(V_0,U_3)}.
\end{aligned}                                               \tag{1.7}
\]

Thus (1.4) is a portal macro with root current
`e_(V1)-e_(V0)`.  It is not a feasible closed one-copy master circuit
without an exterior return.

Adding the omitted seam reversal closes both endpoint currents.  The full
square vector is

\[
 \boxed{
 \Delta_{HO}^{C_4}
   =\sum_{i\in\mathbb Z_4}
       \left(e_{(V_i,U_i)}-e_{(V_{i+1},U_i)}\right).}       \tag{1.8}
\]

It has zero root, owner and q1 marginals.  Support four is minimal on the
simple Boolean-diamond orientation-flip face.  It is not the global ae88
minimum: the independently frozen literal common-live minimum is support
three at roots `81,437,1380`.

## 2. Exact ae88 DM separator with deletions

Let `G_0` be the authenticated ae88 both-live root--owner graph.  Its sizes
are

\[
 |E_0|=1929,\qquad \nu(G_0)=1141,qquad
 (P^+,O^+)=(306,17),                                      \tag{2.1}
\]

and its DM regions are

\[
 \mathrm{PLUS}=306/17,\quad
 \mathrm{CORE}=1011/1011,\quad
 \mathrm{MINUS}=113/402.                                 \tag{2.2}
\]

For a proposed projected reversal, let `D` be its forward columns and `A`
its reverse columns, and use set semantics

\[
                         E'=(E_0\setminus D)\cup A.         \tag{2.3}
\]

An overlay is **source-supported** when `D subset E_0`; it is **strict**
when additionally `A cap E_0` is empty.  These definitions concern the
projected graph only.

Every old maximum-deficiency shore is represented by a forward-closed DM
component family containing all PLUS components and no MINUS component.
Put

\[
                         B=17+1011=1028.                   \tag{2.4}
\]

Use the closure network

```text
source -> component C       capacity |O_C|, C in PLUS or CORE
source -> PLUS component    capacity 2861
MINUS component -> sink     capacity 2861
component C -> component D  capacity 2861 for each old condensation arc
component C(q) -> owner o   capacity 2861 for every final edge (q,o) in E'
owner o -> sink             capacity 1.
```

For a source-side closed family with root shore `R`, its finite cut value is

\[
  B-|N_0(R)|+|N_{E'}(R)|
   =1028+\kappa(R).                                       \tag{2.5}
\]

Therefore

\[
 \boxed{\operatorname{mincut}\ge1029}                    \tag{2.6}
\]

is equivalent to crossing every current deficiency-289 shore after charging
all lost distinct owners.  A fresh matching is still mandatory: a formerly
near-tight shore can become the new blocker.  Exact gain is

\[
                         \nu(E')\ge1142.                   \tag{2.7}
\]

If a fixed old matching loses `s` edges, an augmentation replay must find
`s+1` augmentations, not merely one.

## 3. Exhaustive square audit

The cyclic action on nontrivial subsets of `Z_17` is free.  The complete
rotation-normalized square class has

\[
 {\binom{17}{6}\over17}(11)_4
   =728\cdot7920
   =5,765,760                                               \tag{3.1}
\]

based oriented tuples.  The audit rejects root-, lower- and owner-orbit
collisions before testing both source orientations.

The exact transcript gives:

| class | distinct source-supported overlays | all-old-tight pass | fresh matching gain |
|---|---:|---:|---:|
| closed `C4` | 795 | 3 | 3 |
| open three-edge portal | 20,396 | 591 | 589 |

The two open-portal columns are not circuit witnesses because of (1.6).
The three closed projected gains all have matching `1142`:

| candidate | source | deleted columns | added columns | old matching losses | strict |
|---:|---|---|---|---:|---|
| 8711 | forward | `(131,51),(181,64),(200,78),(143,76)` | `(143,51),(131,64),(181,78),(200,76)` | 4 | no |
| 14544 | reverse | `(757,1245),(363,174),(365,1247),(1253,1105)` | `(363,1245),(365,174),(1253,1247),(757,1105)` | 2 | yes |
| 18154 | forward | `(644,941),(1331,1252),(1312,1152),(1162,1350)` | `(1162,941),(644,1252),(1331,1152),(1312,1350)` | 1 | no |

An explicit 1,142-edge matching is frozen for each row.  Such a matching
also proves every old deficiency-289 shore gains at least one owner: Hall in
the final graph gives `|N'(R)|>=|R|-288`, whereas an old tight shore has
`|N_0(R)|=|R|-289`.

These witnesses do not identify four incumbent ae88 flags whose simultaneous
replacement realizes exactly the displayed graph delta.  They are therefore
proof-safe DM prices and projected matchings, not rooted flag circuits.

## 4. The exact fourteen-owner reset-return vector

For the `k=17,d=3` q1/attachment interface, instantiate the complete-reversal
theorem at owner rank `r=9`.  Let

\[
                         Z=(Z_0,\ldots,Z_{13})              \tag{4.1}
\]

be its rank-nine owner cycle and put

\[
                         I_i=Z_i\cap Z_{i+1}.               \tag{4.2}
\]

The `I_i` are rank-eight ae88 root resources.  Reverse minus forward gives

\[
 \boxed{
 \Delta_{\rm att}^{14}
   =\sum_{i\in\mathbb Z_{14}}
       \left(e_{(I_i,Z_i)}-e_{(I_i,Z_{i+1})}\right).}       \tag{4.3}
\]

It is one alternating 28-cycle with zero rank-eight-root and rank-nine-owner
marginals.  The lower-q1 and upper palettes are unchanged because the two
phases orient the same undirected Johnson cycle.

Opening edge `c` removes its two signed columns.  The rank-eight-root
marginal remains zero, while the owner marginal is

\[
                         e_{Z_{c+1}}-e_{Z_c}.               \tag{4.4}
\]

Thus the open packet is an attachment portal against a fixed exterior.  If
the entire completed state is globally reflected, (4.4) is transported with
the exterior and is gauge rather than a local defect.

The authoritative complete-reversal theorem already proves the packet's
globally simple q1 palettes, positive-run residence, nonempty maximal
antecedent, exact reversal of every derivative inventory and every cyclic
interval-OR width, internal compiler transport, and literal one attachment
plus two predecessor returns.  None of those internal rows is reopened
here.  The overlay audit below asks only whether this proved packet is
literally planted in the authenticated ae88 attachment state.

The raw rotation-normalized parameter class has

\[
 10,461,394,944,000                                        \tag{4.5}
\]

tuples.  The O3 audit enumerates it by a proof-safe pruned DFS: at every
partial reset/rail step it retains exactly those directions with at most one
missing source column.  It freezes every stage count, the full 1,717-component
DM vertex map, and all 785 condensation arcs.

The clean source-supported result is:

| class | distinct overlays | all-old-tight pass | matching gain | maximum matching |
|---|---:|---:|---:|---:|
| closed reset-return reversal | 20 | 0 | 0 | 1141 |
| one-cut open portal | 7,114 | 0 | 0 | 1141 |

There are zero incumbent-phase hits: no clean source-supported owner cycle
has one phase equal to the fourteen corresponding ae88 incumbent flag rows.
Consequently there is no direct literal packet switch at ae88 in this
enumerated class.  A new co-designed flag factor is not excluded.

## 5. Exact `ML_9` protected-path f-factor

Open one owner edge.  The common undirected incidence path has

\[
 14\text{ rank-nine roots},\quad13\text{ rank-eight lower colours},
 \quad26\text{ fixed incidences}.                         \tag{5.1}
\]

The middle-levels graph `ML_9` has

\[
 \binom{17}{8}=\binom{17}{9}=24,310                       \tag{5.2}
\]

vertices on each shore.  If `P` is the protected path, define residual
demands

\[
 b(I)=2-\deg_P(I),\qquad b(Z)=2-\deg_P(Z).                \tag{5.3}
\]

Their common total is

\[
                         \sum b=48,594.                    \tag{5.4}
\]

The exact residual network is

```text
source -> rank-8 I       capacity b(I)
I -> Z                   capacity 1 for every unprotected containment I subset Z
rank-9 Z -> sink         capacity b(Z).
```

### Theorem 5.1 (exact finite q1 host)

For every one of the fourteen packet cuts, the network has flow

\[
                         48,594/48,594.                    \tag{5.5}
\]

Adding the 26 protected incidences gives a spanning two-factor of `ML_9`.
For the canonical cut, the frozen factor has exactly 48,620 distinct legal
containment edges and degree two at every one of the 48,620 vertices.

#### Proof

Integral bipartite flow is equivalent to the residual f-factor equations
(5.3).  The H100 computation saturates every source and sink demand for each
cut.  The materialized canonical factor was independently checked for
containment, uniqueness, all vertex degrees, and inclusion of exactly the
26 protected path edges.  This is a literal positive certificate, not a
counting heuristic.  \(\square\)

The produced factors have between 3,159 and 3,172 components.  Thus this
theorem closes the q1 f-factor only; it does not close topology.

## 6. Exact cut collateral, `d`-overlap and twin banks

### 6.1 The older restitution audit

Let `A=(A_0,...,A_13)` be the maximal depth-three source of the owner cycle.
Each `A_i` has rank six.  A bare cut loses at least fifteen packet-unique
short values with histogram

\[
\begin{array}{c|ccccc}
\text{rank}&7&8&9&10&11\\ \hline
\text{forced loss}&1&2&3&4&5.
\end{array}                                                \tag{6.1}
\]

Appending the canonical first `d=3` source cells makes all fourteen owner
windows literal.  It does not restore the full deck.  The exact remaining
forced short family is

\[
 \boxed{|C_{\rm short}|=3,qquad
        \operatorname{rankhist}(C_{\rm short})=10^1 11^2.} \tag{6.2}
\]

Across all cuts, exhaustive interval replay gives between 12 and 18 missing
cyclic values after the three-cell tail.  For canonical cut zero the three
short masks are

```text
33279, 33791, 49663
```

and the complete residual has 18 masks.  Every cut's literal mask list and
rank histogram is frozen in `run.cuts.tsv`.

This calculation is proof-safe for that declared three-cell tail, but it is
not the sharp local repair and is no longer a list of live ambient-provider
obligations.

### 6.2 The sharp coefficient-one repair

Let `M=4d+2`.  Replace the bare order by the coefficient-one overlap

\[
 \widetilde A=(A_{M-d},\ldots,A_{M-1},A_0,\ldots,A_{M-1}). \tag{6.3}
\]

The `M` length-`d+1` source windows are exactly the `M` cyclic packet owners,
each once.  All lower and middle targets survive.  If `r` is the owner rank,
the exact leave consists of two disjoint Ferrers triangles

\[
 \{Z^X_{i,j}:0\le i\le j<d\}
 \mathbin{\dot\cup}
 \{Z^U_{i,j}:0\le i\le j<d\},                            \tag{6.4}
\]

with rank/multiplicity rows

\[
 (r+t)^t,\qquad (r+d+t)^t\quad(1\le t\le d).             \tag{6.5}
\]

Hence the leave has exactly `d(d+1)` upper targets and none of rank at most
`r`.  At `d=3,r=9`, (6.5) is precisely the twelve-target histogram (0.2).

An explicit Johnson path `P_X` on `2d` owners witnesses the first triangle,
and a path `P_U` on `3d` owners witnesses the second.  Under the four-marker
hypothesis their combined ledger is

\[
 \begin{array}{c|c}
 \text{resource}&\text{count}\\ \hline
 \text{owners}&5d\\
 \text{lower q1 colours}&5d-2\\
 \text{upper q1 colours}&5d-2\\
 \text{duplicated upper targets}&d(d+1).
 \end{array}                                              \tag{6.6}
\]

Two one-sided `d`-root Ferrers collars add at most `2d` owners, so the
collared bank uses at most `7d` reserved owner positions.  Every interval
witness is literal after maximal erosion, and the entire object is common
under global reversal.  It uses the `d` boundary positions already required
by a linear depth-`d` factor; it appends zero source letters.

Consequently, once a selector chooses a *validated protected twin-bank
embedding*, it activates no provider variables for (6.4): their witnesses
are already fixed intervals of the two selected banks.  Ambient-provider
Hall rows remain relevant only for exterior losses not covered by (6.4).

For comparison, if one deliberately uses an unbanked option `eta`, let
`C_eta` be its complete loss and `P_eta(S)` the permitted ambient providers.
Then the exact fallback rows are

\[
 \sum_{a\in P_\eta(S)}u_{\eta Sa}\ge h_\eta
       \qquad(S\in C_\eta),                               \tag{6.7}
\]

together with the occurrence/cell capacity rows.  If providers must be
distinct, Hall separation on the target--provider graph is exact.  The
three targets in (6.2) are only the forced short subfamily of the older
tail; (6.7) must charge its complete 12--18-target ledger.

### 6.3 Exact `k=17` applicability boundary

For ae88, `d=3,r=9`, so `h=2` and the normal form occupies sixteen of the
seventeen coordinates.  The leave theorem (6.4)--(6.5) is valid.  The
four-marker protected separation theorem is not: `P_X` can use the two
available markers, but the stated `P_U` construction calls for two further
permanent markers.  The one unused ambient coordinate cannot supply both,
and promoting it to `H` changes the owner rank.

Nor can the two markers simply be reused.  If
`(eta_3,eta_4)=(eta_1,eta_2)`, the Section-5 bank formula gives
`M_(d+1)=L_0`; swapping them gives `M_(d+1)=R_0`.  In either case the two
banks share an owner, so coefficient-one owner disjointness fails.  The
four-marker hypothesis is load-bearing for the frozen formulas.

Similarly, the generic combined protected-factor threshold is

\[
                         m\ge22d=66,                       \tag{6.8}
\]

whereas the exact graph here is `ML_9`.  Section 5 therefore certifies the
opened packet path only.  A literal `h=2` bank redesign or an exact
marker-tight protected embedding/f-factor is a separate finite gate.  Even
such a physical embedding must still pass the `C_17` orbit-collision,
literal option/state, and rebuilt common-live tests.

## 7. Proof-safe one-oriented co-design master

Let `h_eta` choose a prevalidated packet embedding, opening and, when used,
its overlap, twin banks and collars.  Let `y_(q,f)` choose literal rooted
flag option `f` at root `q`; let `a_(q,o)(y)` be the **rebuilt** both-live
root--owner incidence, and let `theta_(q,o)` be the final attachment
matching.  A compact exact master has

\[
 \sum_\eta h_\eta=1,
 \qquad \sum_f y_{qf}=1,                                  \tag{7.1}
\]

together with the exact resource/type rows of the flag factor.  Template
columns include their actual rooted-row changes and every reserved
owner/lower/upper-q1 position.  In particular, (4.3) is not itself a flag
column and a twin bank is not represented only by its twelve target labels.

### 7.1 Attachment matching and incumbent survival

Use integral variables satisfying

\[
 \theta_{qo}\le a_{qo}(y),\qquad
 \sum_o\theta_{qo}\le1,\qquad
 \sum_q\theta_{qo}\le1,
 \qquad \sum_{q,o}\theta_{qo}\ge1142.                    \tag{7.2}
\]

This is the exact gain target beyond deficiency `289`.  If the branch also
requires literal survival of a frozen incumbent matching `M0`, impose

\[
                         a_e(y)=1\qquad(e\in M_0).         \tag{7.3}
\]

The deletion-aware old-DM network of Section 2 is the exact Benders pricing
separator for all old maximum shores; a fresh max-flow for (7.2) is the
exact final separator and supplies a violated Hall shore when the target is
missed.  The distinction is essential: old-tight crossing plus (7.3) is a
strong screen, but only the fresh matching certifies `1142`.

For the enumerated square class, each of the three `1142` witnesses deletes
at least one edge of the frozen `M0` (respectively four, two and one).  Thus
no support-four square column satisfies old-tight crossing, gain and (7.3)
simultaneously.  This is a scoped no-go for that circuit class, not a global
support lower bound.

### 7.2 Exact punctured-containment separator

For a selected protected subgraph `P_eta` in `ML_9`, set

\[
 b_\eta(v)=2-\deg_{P_\eta}(v)                             \tag{7.4}
\]

and delete its already fixed incidences from the residual containment graph
`H_eta=(L,R,E_eta)`.  The integral recourse flow is

```text
source -> I in L       capacity b_eta(I)
I -> Z in R            capacity 1 for IZ in E_eta
Z in R -> sink         capacity b_eta(Z).
```

Assuming the two demand totals agree, its exact family of punctured Hall
cuts is

\[
 \boxed{
 b_\eta(X)\le b_\eta(Y)+|E_\eta(X,R\setminus Y)|
 \quad(X\subseteq L,\ Y\subseteq R).}                    \tag{7.5}
\]

Indeed the corresponding cut has capacity
`B-b_eta(X)+|E_eta(X,R minus Y)|+b_eta(Y)`.  A minimum cut therefore either
saturates all demand or returns a violated instance of (7.5); no degree-only
shortcut is used.  Section 5 proves (7.5) for every packet-only opening.
The combined packet/bank row may enter the master only after its own
marker-tight template and (7.5) certificate are frozen.

### 7.3 State max-closure and upper witnesses

For selected state variables `x_s`, retain exactly

\[
 \boxed{M+x(X)-x(\Gamma^+(X))\le1430
                   \qquad(X\subseteq\Omega).}             \tag{7.6}
\]

Its separator is a two-layer maximum closure: give a left copy of `s`
weight `x_s`, a right copy weight `-x_s`, and add infinite arcs from each
left vertex to all right vertices in its `Gamma^+` image.  A finite maximum
closure has value `x(X)-x(Gamma^+(X))`; if it exceeds `1430-M`, its source
side returns the violated Benders row (7.6).

For every validated bank template, each target in (6.4) is linked to its
explicit interval in `P_X` or `P_U`; selecting `h_eta` activates that witness
and reserves all owner/q1 resources in (6.6).  There is no local provider
matching for those targets.  Exterior arbitrary-width targets, path joins,
upper-q1 completion and the terminal compiler remain joint host rows.

Finally choose those downstream witnesses and a compiler matching for one
complete oriented child.  When the induction state and transition are
reversal-closed, reflect the entire source, endpoints, collars, upper
witnesses, cap and compiler for the opposite representative.  This replaces
a fixed-address `G^+ ∩ G^-` compiler overlap.  It does not permit
a selective packet reversal in a frozen exterior and does not alter any
invariant Hall or topology obstruction.

The disjoint second-facet projection theorem may be used for its protected
endpoint tickets only.  Relative to a fixed predecessor matching `M0`, the
condition

\[
             M_0\bigl(\psi(R)\cap\phi(R)\bigr)=\psi(R)     \tag{7.7}
\]

remains open.  Arbitrary head injection can repeat lower intersections.
Thus lower/root, head injectivity, and graphic acyclicity remain one joint
Boolean fixed-`M0` correlation problem.  In particular, the degree-only
rainbow shortcut is unavailable; the Wdowinski counterexamples do not
respect the Boolean fixed-`M0` structure needed here.

## 8. Exact support and remaining boundary

The exact conclusions about support are:

* the open square has three changed edges but is not a closed circuit;
* the closed Boolean-square orientation flip has abstract support four and
  three ae88 graph-level gain embeddings;
* none of those three support-four embeddings preserves the frozen
  incumbent matching edge-by-edge;
* the global minimum literal ae88 common-live gain remains support three;
* a planted clean depth-three complete-reversal packet changes exactly
  fourteen rooted rows; and
* no direct source-supported fourteen-row ae88 planting exists in the
  audited class.

The exact `ML_9` f-factor removes the finite packet-only q1-host question at
`k=17`.  The `d`-overlap/twin-bank theorem removes the local cut-collateral
obstruction whenever its protected template is available; no lucky
`O(1)`-loss cut is needed.  Still open are:

* a literal co-designed ae88/rooted host for the packet;
* a redesigned marker-tight `h=2` twin bank and combined protected `ML_9`
  factor, or an equivalent exact k17 resource-separation certificate;
* rooted Catalan lower--head--graphic correlation;
* embedding and joining the packet plus both banks in one oriented resident
  coefficient-one host;
* upper-surjective decoration, exterior arbitrary-width windows and named
  one-sided socket rows;
* one oriented complete compiler/cap certificate;
* state max-closure `M=1430`, voltage where required, and regeneration.

Global reversal transports any completed solution of these rows.  It cannot
solve an invariant deficiency or a named one-sided socket.

## 9. Frozen artifacts

### ae88 square/reset-rail outer audit

```text
scratch/r2_k17_ae88_square_resetrail_dm_20260801/
  frozen_scope.audit.json
  run.audit.json
  run.candidates.tsv
  run.dm_vertices.tsv
  run.dm_components.tsv
  run.dm_condensation.tsv
  c4_gain.audit.json
  c4_gain.matchings.tsv
  twin_bank_rebase.audit.json
```

The principal hashes are bound in `frozen_scope.audit.json`.  The exhaustive
enumerator source SHA-256 is
`1aae339746b13175dd33f492478d4b7eb94fdba0808c6e7ae36a1df4232beb11`.
The authoritative handoff-2571C rebase is bound by
`twin_bank_rebase.audit.json`, SHA-256
`3cc3fe4e6f7bd8fb281999f0cc146a06fff5b4f384384deed0a4b29dac8bd5f8`.
Its twin-bank theorem input SHA-256 is
`13285f8387a0c43fab9d3fd1a14ac6076bc0646a1fc94a570a1ad36cc4a6e765`.

### exact `ML_9` q1 host

```text
scratch/r2_k17_reset_open_ml9_ffactor_20260801/
  frozen_scope.audit.json
  run.audit.json
  run.cuts.tsv
  run.cut0.factor.tsv
  run.cut0.independent.audit.json
```

The canonical factor SHA-256 is
`74c93d0387e9e833d145464264d7121ec89971f7cb7f509127b192c5b92ee4fa`;
the independent audit SHA-256 is
`82b76c8438ff5514937ef272b7a1f5c530a1494c1924eb37f2d5696149080dde`.

All finite work was CPU-only `g++ -O3 -std=c++20 -DNDEBUG` on `h100` through
`ssh -o ClearAllForwardings=yes`, in the two unique directories recorded in
the manifests.  No heavy local computation or Python was used.
