# Certified closure of the full `k=14` one-hole/two-edit anchor-star portfolio

Date: 2026-07-24

## Result

All 26 immutable shards of the anchor-star portfolio are DRAT-verified UNSAT.
The family consists of all 208 one-hole deletion roots of the certified
242-entry suffix, with common editable anchor position 3 and every one of the
other 240 root positions.  Both values are arbitrary nonzero 14-bit masks.

The checked family therefore contains

```text
49,920 architectures
13,398,662,234,880 root/architecture/value tuples
```

No member yields a universal 3,675-entry word with the frozen 3,434-entry
prefix.  This closes the stated family only and leaves
`3434 <= nu(14) <= 3676` unchanged.

## Certification

Every shard was generated by the independently audited provider-CNF source,
then subjected to its clause-by-clause semantic auditor.  CaDiCaL returned
UNSAT for every formula.  Every corresponding proof was checked independently
by `drat-trim`, and all 26 logs end in `s VERIFIED`.

Shard 00 has 16,043 variables and 191,871 clauses.  The remaining shards have
the same 1,920-case bound and comparable formula sizes.  Each per-shard
`UNSAT_VERIFIED.sha256` file binds its manifest, formula, map, stats, proof,
and checker log.

The complete certificate bundle is
`scratch/k14_onehole_twoedit_shard00_certified_20260724/`.  All 26 per-shard
hash manifests were rechecked locally.  The bundle-wide `SHA256SUMS` has hash

```text
88580a524b3289c4457c1df1f3f53ca62a353d596d7db8f54bfc77b77310500b
```

Shard 00 details and executable hashes are recorded separately in
`K14_ONEHOLE_TWOEDIT_SHARD00_CERTIFICATE_20260724.md`.

## Exact boundary

The certificate excludes only the common-anchor two-edit stars over the 208
one-hole deletion roots.  It does not exclude two arbitrary positions without
the common anchor, three or more edits, a different prefix, or the unrestricted
append-241 formula.  The live unrestricted branch-5 and branch-36 searches are
logically separate.
