# Self-audit: four coatom fans and occurrence-socket replication

**Date:** 2026-08-04  
**Method:** exact symbolic replay against the frozen tensor, occurrence, and
protected-factor theorems; no finite search or solver  
**Verdict:** `GO` at the native occurrence level.  Common-cap embedding and
external product typing remain premises exactly as stated in the source.

## Frozen source

```text
f2c3bb59ea841c16cf5ab9ad1b8f6d893e6126de3426ed059e573f3b0b22c6ae  MATH_THEOREM_COATOM_FOUR_FAN_OCCURRENCE_SOCKET_REPLICATION_20260804.md
```

Input hashes:

```text
eeb8dfe1c04f7bf0e6bb77bf84bfa03ddf2a66749e1d6e39399833928a64bf08  MATH_THEOREM_H2_COATOM_TENSOR_FIXED_ACTIVE_MENU_AND_HOST_QUANTIFIER_20260801.md
2d9fbf37ee6771013a28edd299b69ca5f4123c97d57f46339248e663fe6e2b83  MATH_THEOREM_COATOM_SCREEN_TENSOR_RESIDENT_ECO_PACKET_20260801.md
a455c5804840e7bf13f90b44b4076fa46339a9d8e2247bb192ed140cb7ccb262  MATH_THEOREM_PROTECTED_ML_FACTOR_OCCURRENCE_LIFT_AND_OPENING_PARITY_20260804.md
a7176faaf6cd44768df058423c4f492018fa0f6b256008dd0de80cf3ecf85ef7  MATH_THEOREM_MIDDLE_LEVELS_TURN_DIAMOND_CAPACITY_ROUTER_20260804.md
1deded37f83351f0750e633b8e771bc364159d7b979c21d21c5c306573b5f17f  MATH_THEOREM_FIXED_H_COLLAR_Q1_TWO_FACTOR_AND_ROOTED_HOST_GATE_20260801.md
a6dba7ab6eab0e14e47552ab52ce00a2962969a6168f08972946720c6224fe53  MATH_THEOREM_FOLDED_C8_NESTED_TERMINAL_INVARIANT_AND_POLARIZED_SOCKET_20260804.md
```

## 1. Local fan replay

For `O_i=Z-f_i`,

\[
 O_i\cap O_{i+1}=Z-\{f_i,f_{i+1}\},\qquad
 O_i\cup O_{i+1}=Z.
\]

The deleted singletons distinguish all owners, and the deleted adjacent
pairs distinguish all lower turns.  There are `n-1=delta+1` turn addresses.
The occurrence theorem explicitly retains the turn index in a q1 node, so
equal Boolean values at these addresses do not identify their physical
occurrences.

## 2. Routing replay

Turn `i-1` uses owner `O_i` precisely under choice `1`; turn `i` uses the
same owner precisely under choice `0`.  Thus the only collision pattern is
`epsilon_(i-1)epsilon_i=10`.  Avoiding every such pattern is equivalent to
a nondecreasing binary word.  Length `n-1` gives `n` cut positions.  Sources,
selected owners, and occurrence-labelled sinks are then all distinct.

This verifies literal route disjointness in the native turn-diamond complex.
It does not identify that complex with a residual common-cap graph.

## 3. Four-block replay

Comparing the authoritative phase words positionwise gives the common block
indices `1,4,5,12`, with active triples `Iab,Cd,Ic,ab`.  Coatom expansion
uses the same filler order at corresponding positions.  The tensor's
simple-owner theorem and exact lower-rainbow theorem rule out cross-block
owner and lower collisions.  Different block/turn addresses rule out
terminal-occurrence collisions.

The four active triples are distinct, and the tensor core/filler is disjoint
from the active universe.  Therefore their four complete block-union values
are distinct.  This proves `4(delta+1)` distinct source and sink occurrences
and four value fibres of multiplicity `delta+1`.

## 4. Protected-factor replay

One alternating fan path has `delta+1` lower vertices and hence
`2(delta+1)` incidence edges.  Four vertex-disjoint fans have
`8(delta+1)` edges and maximum degree two.  Under the displayed incumbent
degree compatibility and total edge bound `<=m-2`, the small
protected-factor theorem applies exactly.  At every selected lower, its two
protected edges exhaust degree two, forcing the displayed q1 turn.

The proof does not claim Hamiltonicity, safe linear opening, upper
decoration, or residence outside the original complete tensor.

## 5. Polarized-socket and Hall replay

In a fixed disjoint fan routing, each used owner occurrence and its q1 sink
are distinct and have nested Boolean values.  Across routes these pairs are
capacity-disjoint.  They therefore instantiate exactly the native-pair row
of the conditional polarized-socket theorem; exact upstream witnesses and
state acceptance remain hypotheses.

For `h` tickets each having at least `h` neighbours in the fixed route bank,
every nonempty ticket family `X` satisfies

\[
 |N(X)|\ge h\ge |X|.
\]

Thus Hall's theorem gives the displayed fixed-task matching corollary.  No
independence of the ticket neighbourhoods is assumed.

Because all four blocks and all their filler addresses are literally
identical in the two packet phases, the identity map preserves the complete
native fan complex.  The phase-regeneration corollary is therefore exact
for predicates factored through that phase-common record.  It correctly
does not assert that an arbitrary external type predicate is phase-blind or
that one occurrence may be used twice.

For `H` vertex-disjoint packet banks, edge and route counts add exactly:
`8H(delta+1)` protected edges and `4H(delta+1)` disjoint routes.  The
small-factor implication is conditional on both vertex-disjointness and the
displayed total edge bound; private common skeletons remain an explicit
prospective-packing premise.

## 6. Capacity and type boundary

The source correctly distinguishes:

- an occurrence model, where the `delta+1` turn addresses are distinct unit
  capacities;
- a value quotient, which must retain aggregate capacity `delta+1` rather
  than silently collapsing to one; and
- the residual common-cap network, into which the occurrence nodes still
  require an exact typed embedding.

Nothing in the source replaces incomparable folded-ray target identities by
a nested native chain.  It supplies physical terminal multiplicity only.
Upstream witnesses, polarity acceptance, task binding, compensation
survival, whole-packet planting, and regeneration remain explicitly open.

With those scope boundaries, every asserted count and implication is exact.
