# Independent audit: K17 drop-12 no-H S47 bilateral frontier and launch schema

**Date:** 2026-08-03

**Verdict:** **GO for the mathematical decision tree and producer schema;
PRELAUNCH for computation.**  The existing `S,S,N` executable is a complete
unreduced physical-menu oracle, but it is not yet frozen as the reduced
bilateral run.  No general no-H S47 bilateral producer exists, and the
available bounded triple consumer authenticates H468 sources rather than
S47.  No broad run is authorized by this audit.

## 1. Exact bindings

This audit is bound to

```text
compressed parent  e878bf19654d8478b4552451a3b5c9a54716e1383d2513c59d680091e10b5ffb
literal final      fa49188250bf194c8218d8afb5bf5f9220e4a73fd1267824bc7ed0b2868bbb7c
structural edges   790fae940cd80c05e35656027dcb57139180b54c77f567c9b5333ab5d4581434
all-U classes      160fd9c41409ef8bc99977efbeae4a5e45b8da4c492792cdb0ba4d7273d9f58c
global H/S theorem 7556ec953b062a88810b81f26163ff8c86616019dbaa44a6ac9ce1216d1b30ca
global H/S audit   17c9cc3ca53cf8fbda08f2d0cce4faf44d57e21169c2829a8f3e1cc53836c8ec
S unary ledger     8311bb9909d22e8361c50d700909c46ed9e287df1aa2f91842d5328068c6c8c2
support-one theorem
                    a2395a13cd992ab1b14fbb5192cb88ae0083646f28c4099269c2b9c90e36c058
support-one audit   f1744079aca3d5ecd4488f6abc723ab529398037f471e7483d04ee165673f4f2
Task2 final manifest
                    13f09c24b3ab43ab4843a4a6c702e105749096a1cd1ce8394f8f659b8923629b
S-by-N audit        2a33d3e228b21022a7e8631c914de7f0e3e1e5c09936b71e608a550b85c53997
no-H S theorem      c72638af58777ba96481a9ddf7e7fd8769aca73ba824fa14eeeed48d0bd8eb10
```

The no-H theorem now cites the current `a2395a13...` support-one theorem,
not its earlier `066093c9...` snapshot.  The latter remains legitimate only
inside packages that copied and hash-bound that exact older file.

## 2. Shortest exact decision tree

Fix an endpoint-disjoint three-mode selection on the bound parent and ask
whether final supplier deficiency can be at most 20.

1. The exact inherited-Q23 source cover forces an `H` or `S` coordinate.
   If the child contains `H`, route it exclusively to the H-owned lane.
2. On the no-H face, write `s` and `n` for the numbers of `S` and `N`
   coordinates.  The exact necessary inequality is
   `gamma_Q <= s-n`, all `Z` coordinates are Q identities, and at most one
   `N` can occur.  Hence the only target-capable class patterns are

   ```text
   SZZ  SSZ  SSS  SSN.
   ```

   In particular, `SZN` is rejected before occurrence pricing.
3. The authenticated unary, complete S-by-(S or Z) prefix, 334,639-row
   third-shore replay, and 329-row S-by-N replay together prove that **every
   S role** in an occurrence-valid retained triple uses both other selected
   hosts across its two phase tickets.
4. Therefore the required bilateral-source label multiplicities are

   | physical class | S labels that must be retained | exact two-phase host templates |
   |---|---:|---:|
   | `SZZ` | 1 | 645 |
   | `SSZ` | 2 | 84 |
   | `SSS` | 3 | 8 |
   | `SSN` | 2 | 84 |

   The template counts were independently recomputed from the 61 legal
   one-phase host assignments.  They are not physical-child counts.
5. For each retained S label, build only the nine exact bilateral phase
   joins

   ```text
   (B,G2) (G2,B)
   (G1_f,G1_g) (G1_g,G1_f)
   (G1_f,G2) (G2,G1_f)
   (G1_g,G2) (G2,G1_g)
   (G2,G2).
   ```

   Intersect the resulting source-option relations by physical child before
   helper-menu expansion: one nonempty bilateral S bank is necessary for
   `SZZ`, two for `SSZ/SSN`, and three for `SSS`.
6. Regenerate complete triple-local menus for all three roles.  Join the two
   phases on the same key within each role, allow different keys between
   roles, enforce phase-local unit row capacity, and require one global
   physical-row flag map.
7. Only an exact occurrence positive proceeds to simultaneous three-transfer
   materialization and a fresh complete supplier matching.  The acceptance
   threshold is rank 16,878; inherited-Q23 credit is pruning only.

For `SSN`, the two S roles both use N host row 14,851.  Unit phase capacity
forces complementary one-phase uses at one common flag, and the N role must
avoid row 14,851 in both phases.  These are exact necessary checks, not
heuristics.

## 3. Authenticated finite domains

The complete frozen `S,S,N` structural census is

```text
raw C(47,2)*7                         7,567
endpoint-disjoint physical children  5,705
retained S labels                    11,410
Q23-admissible physical children     4,676
Q23-admissible S labels               9,352
Q23-rejected children                 1,029
```

Its domain/orientation ledgers have SHAs
`d73e0a01f4fbfc0b6227a2339e13cb65d0a1b42102231329c9091310cb9b295a`
and
`ca5c609573c9b0fa9cdef72f4d89c646995fe301982969dcc543bbac3d57c8be`.
The census manifest file SHA is
`ca768c37c1733570d5f161b0d889dfe140eda5c8559baa62afdec9d440495729`;
the independent boundary manifest file SHA is
`93daf7e0f52fb79fd406094a65ac42be6ef1e08e6d49cc5fde0348f807a31835`.

No authenticated physical-child census is currently frozen for `SZZ`,
`SSZ`, or `SSS`.  Their `645/84/8` values are only host-incidence template
counts and must not be reported as domain sizes.  The observed S47 cache key
has ten rows and 47 exact postings, ledger SHA
`1344cb8e8903d7352c522906278cd3671692e2a9c6118d86c096bddd83e20d38`.
Its row-set reconstruction is exact, but predicate truth-vector equivalence
has not been independently proved, so it remains an optional disabled cache.

## 4. Source audit: existing SSN verifier

The source

```text
scratch/k17_drop12_SSN_subface_20260803/
  verify_k17_drop12_SSN_subface_exact_20260803.cpp
SHA-256 23c381a03ae9241dcc084a2ae7f663835c0f4fd900d6012042635e9351966f0e
```

passes its complete 27-entry census manifest and local self-test.  Static
inspection confirms that its `verify` mode is an exact **unreduced** oracle:

* it authenticates the parent, catalogue, S47 and all-U class ledgers;
* applies the exact simultaneous Q23 filter;
* deletes all three donors and inserts all three hosts in both owner phases;
* enumerates all 90 keys and every physical predecessor/successor tuple for
  all three roles;
* uses 20/20 phase reservations, intersection 17, union 23, and the 7,213
  private rows;
* joins each role internally on one key and consistent flags;
* enforces pairwise same-phase footprint disjointness and a global flag map.

Thus failure to use the new bilateral theorem cannot cause a false negative.
However this source is **not frozen for a promoted reduced run**:

1. its comments and emitted scope still say support-one pruning is open;
2. it binds neither the current support-one theorem/audit nor the Task2 and
   S-by-N closure packages;
3. a positive asserts only `lo_support != 0 && hi_support != 0`, whereas the
   frozen theorem requires both masks to equal `3`;
4. it does not assert the complementary phase split at row 14,851 or the
   N-role avoidance of that row; and
5. only a census manifest exists.  There is no checked verify-run manifest
   binding a command, compiler, binary, accepted shard, outputs, stdout and
   stderr.

A proof-safe launch may either patch and independently audit those bilateral
checks before pricing, or run the unreduced exact oracle and independently
post-audit every positive for them.  In both cases, first freeze a single-row
runtime/output calibration and a non-overlapping shard plan.  This audit
does not authorize that run.

## 5. General S producer status

`MATH_THEOREM_K17_DROP12_NOH_S47_BILATERAL_TRIPLE_FACTORIZATION_20260803.md`
gives a proof-safe `B/G1/G2` schema, including both helper-donor casualty
tests, legal equal-flag `(H_f,H_f)` G1 self tickets, G2 orientations, full
mode/state provenance, and label-preserving child projection.  No matching
general S producer source exists to audit.  H468's 32-class producer and G2
empty result are not transferable to S47; S47 has ten observed cache values
and no proved G2 emptiness.  The general lane therefore remains
`PRELAUNCH_NO_SOURCE`.

## 6. Positive consumer status

The bounded consumer at

```text
scratch/k17_drop12_bounded_triple_supplier_consumer_20260803/
  consume_k17_drop12_occurrence_positive_triple_20260803.cpp
SHA-256 2fe7c0b2a94e7811525878a67ed59b463a30b9dd5c6b973cc62120531eaa9ba9
```

correctly implements simultaneous materialization, six-ticket replay,
protected-row checks, and full supplier matching, but its source parser is
hard-coded to the 468 H modes and singleton rank 16,878.  It rejects an S47
source and is only calibration for this lane.

An S-capable consumer must authenticate all three selected edges against the
fixed-bank-safe U ledger, authenticate every designated source label against
class `S`, and otherwise retain the existing exact checks:

```text
six distinct non-incumbent endpoints;
host [u,q] -> [l,u,q], donor [l,m,r] -> [m,r] simultaneously;
7,213 private rows, 23 occurrence rows and 20 incumbent endpoints unchanged;
six literal new tickets plus the ten incumbent phase tickets replayed;
one child table SHA; complete supplier graph/matching/Hall/zero ledgers;
accept iff matching >= 16,878.
```

## 7. Scope

This audit closes no bilateral occurrence face.  It reports no SZZ/SSZ/SSS
physical census, no occurrence positive, no supplier-deficiency-20 child,
and no higher-arity, chronology, residence, compiler, or K17-word result.
Phase 1 remains the transported owner phase only.
