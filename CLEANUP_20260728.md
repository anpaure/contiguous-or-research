# Workspace cleanup — 2026-07-28

The workspace was reduced from 56 GiB (as measured by `du -sh`) to 9.3 GiB.

Removed:

- 17,126 uncompressed, top-level `scratch/*.cnf` search instances;
- 49 top-level compressed SAT-input archives matching `scratch/*.cnf.zst`,
  `scratch/*.cnf.gz`, or `scratch/*cnfs.tgz`;
- Python `__pycache__` directories and writable `.DS_Store` files.

Preserved:

- all source files, mathematical notes, and `MATHEMATICAL_HANDOFF.md`;
- JSON certificates, manifests, verifiers, hashes, and current k=15 artifacts;
- proof/certificate subdirectories, including their CNF/DRAT inputs;
- root-level historical CNFs, including the handoff-referenced
  `k10_factor_full.cnf`;
- non-CNF compressed proof archives such as the k=11 deletion proofs;
- the current Thread-D four- and five-parent pattern binaries.

Deletion inventories (size, timestamp, original path):

- `CLEANUP_20260728_SCRATCH_DIRECT_CNF_MANIFEST.tsv`
- `CLEANUP_20260728_SCRATCH_COMPRESSED_CNF_MANIFEST.tsv`

All deleted files were generated solver inputs rather than irreplaceable
certificates. They can be regenerated from the retained sources if a legacy
experiment is ever revived.
