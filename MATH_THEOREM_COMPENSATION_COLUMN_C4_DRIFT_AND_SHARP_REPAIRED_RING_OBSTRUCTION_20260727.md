# Retracted compensation-column \(C_4\) obstruction

Date: 2026-07-27

This draft is retracted.

The original argument made two invalid identifications:

1. it used the edge influence cap \(a(e)\le A\) as a bound on the
   resource pair-link \(p(y)=d_t(X,y)\), although \(c(e,y)>0\) does not
   imply \(p(y)\le A\) when \(y\notin e\); and
2. it counted same-resource column collisions inside the positive
   Taylor correction, although the exact generator places
   \(e\cap g\ne\varnothing\) and \(y\in e\) in protected-index death.

The corrected theorem, including

- the exact starred edge and legal coin generators,
- the two-colour compensation breadth theorem with separate caps
  \(a(e)\le A\) and \(p(y)\le P\),
- the complete equality/witness-partition classification,
- a literal legal coin obstruction,
- a literal separated-endpoint starred-\(C_4\) obstruction, and
- the surviving weighted regeneration gate,

is:

MATH_THEOREM_STARRED_C4_COIN_ENDPOINT_AND_EDGE_ONLY_WEIGHTED_GATE_20260727.md.

Only that corrected note should be cited.
